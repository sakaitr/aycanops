# Finans Modülü — Faz 1 Implementasyon Planı

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** AycanOps'a finans modülünün temelini kurmak — hesap planı, kategori, masraf
merkezi, proje, kasa/banka hesabı, para birimi/kur, vergi kodu, ödeme yöntemi, belge
türü tanımları; genel gelir-gider kayıt modülü; ve personelin harcama öncesi onay
istediği masraf talebi akışı.

**Architecture:** Next.js App Router + MySQL (mysql2/promise) üzerine kurulu mevcut
AycanOps deseninin birebir devamı: her varlık için `migrations/NNN_*.sql` + REST-benzeri
`app/api/**/route.ts` (GET/POST) + `app/api/**/[id]/route.ts` (PUT/DELETE) + `"use client"`
sayfa (`app/**/page.tsx`, liste + slide-over form). Yetkilendirme `lib/permissions.ts`
kataloğuna yeni `finans_*` kaynakları eklenerek yapılır, mevcut dinamik rol sistemiyle
(migrations/075) otomatik uyumludur — yeni izinler DB'deki `role_permissions` tablosuna
seed sırasında değil, `DEFAULT_ROLE_PERMISSIONS` üzerinden mevcut sistem rollerine
(yonetici/admin) dağıtılır; ilk `loadPermissionsCache()` çağrısı roller tablosu zaten
doluysa DEFAULT'ları tekrar seed etmez — bu yüzden yeni izinlerin mevcut rollere de
`role_permissions` tablosuna elle eklenmesi gerekir (Task 2'nin son adımı).

**Tech Stack:** Next.js 16 (App Router, Turbopack), TypeScript, MySQL/MariaDB
(mysql2/promise, `lib/db.ts`'teki `getDb()` sarmalayıcısı), Zod (`lib/schemas.ts`),
Tailwind (inline class'lar, mevcut zinc-950 koyu tema).

## Global Constraints

- Bu depoda birim test çalıştırıcısı (jest/vitest) **yok** — sadece Playwright (e2e,
  ayrı bir süreç). Her task'ın doğrulaması: (1) `node node_modules/typescript/bin/tsc
  --noEmit` sıfır hata vermeli (normal `npx tsc` bu ortamda `EACCES` verebilir, bu
  yüzden doğrudan `node .../tsc` kullan), (2) belirtilen curl/DB komutlarıyla veya
  canlı tarayıcıda (dev server + Playwright/manuel) gerçek bir uçtan-uca kontrol.
- Tüm id kolonları `VARCHAR(36)`, tüm tarih-saat kolonları `VARCHAR(30)` (ISO string,
  `lib/time.ts`'teki `nowIso()` ile üretilir — gerçek DATETIME kolonuna yazılmaz, bu
  depodaki yerleşik kural). Sadece "takvim tarihi" alanları (vade, dönem başlangıç/
  bitiş gibi) gerçek `DATE` kullanır.
- Her yeni tablo `ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci`.
- Migration dosyaları `migrations/NNN_*.sql` — bir sonraki boş numara **076**.
  `lib/migrate.ts` dosya adını sıralı okur ve `execScript` (multi-statement) ile
  çalıştırır; `DROP CHECK`/`DROP CONSTRAINT` bu ortamda güvenilir çalışmıyor (bkz.
  migrations/075) — bu plan hiçbir yerde bunu kullanmıyor.
- Her API route dosyası şu üçlüyü izler: `requireUser()` → 401 kontrolü →
  `hasPermission(user, "kaynak:aksiyon")` → 403 kontrolü → iş mantığı → `apiError(e)`
  catch bloğu (bkz. `lib/api-error.ts`).
- Dev sunucusu ve DB tüneli önceden kurulu olmayabilir — her task'ın doğrulama adımı
  önce `nc -z -w3 127.0.0.1 3307` ile tüneli kontrol etmeli, düşmüşse
  `pkill -f "ssh.*3307"; sshpass -p 'kayra123' ssh -o StrictHostKeyChecking=no -o
  ServerAliveInterval=15 -o ServerAliveCountMax=3 -f -N -L 3307:172.22.0.3:3306
  root@212.64.201.150` ile yeniden bağlanmalı, sonra dev sunucusunu
  `pkill -f "next dev"; cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
  && nohup npm run dev > /tmp/aycanops-dev.log 2>&1 &` ile başlatmalı.
- Prod DB üzerinde çalışıyoruz (ayrı bir dev/staging DB yok) — migration'lar dışında
  hiçbir doğrudan `DELETE`/`UPDATE` prod verisine uygulanmaz; test verisi eklenirse
  (Task 8, 10, 11'in canlı testinde) test sonunda temizlenir.

---

## Dosya Yapısı Özeti

```
migrations/076_finans_temel_tanimlar.sql   (yeni — Task 1)
lib/permissions.ts                          (değişiklik — Task 2)
lib/schemas.ts                              (değişiklik — Task 3)

app/api/finans/hesap-plani/route.ts         (yeni — Task 4)
app/api/finans/hesap-plani/[id]/route.ts    (yeni — Task 4)
app/admin/finans/hesap-plani/page.tsx       (yeni — Task 4)

app/api/finans/kategori/route.ts            (yeni — Task 5)
app/api/finans/kategori/[id]/route.ts       (yeni — Task 5)
app/admin/finans/kategori/page.tsx          (yeni — Task 5)

app/api/finans/masraf-merkezi/route.ts      (yeni — Task 6)
app/api/finans/masraf-merkezi/[id]/route.ts (yeni — Task 6)
app/admin/finans/masraf-merkezi/page.tsx    (yeni — Task 6)
app/api/finans/proje/route.ts               (yeni — Task 6)
app/api/finans/proje/[id]/route.ts          (yeni — Task 6)
app/admin/finans/proje/page.tsx             (yeni — Task 6)

app/api/finans/kasa-banka/route.ts          (yeni — Task 7)
app/api/finans/kasa-banka/[id]/route.ts     (yeni — Task 7)
app/admin/finans/kasa-banka/page.tsx        (yeni — Task 7)

app/api/finans/vergi-kodu/route.ts          (yeni — Task 8)
app/api/finans/vergi-kodu/[id]/route.ts     (yeni — Task 8)
app/admin/finans/vergi-kodu/page.tsx        (yeni — Task 8)
app/api/finans/odeme-yontemi/route.ts       (yeni — Task 8)
app/api/finans/odeme-yontemi/[id]/route.ts  (yeni — Task 8)
app/admin/finans/odeme-yontemi/page.tsx     (yeni — Task 8)

app/api/finans/gelir-gider/route.ts         (yeni — Task 9)
app/api/finans/gelir-gider/[id]/route.ts    (yeni — Task 9)
app/finans/gelir-gider/page.tsx             (yeni — Task 9)

app/api/finans/masraf-talebi/route.ts       (yeni — Task 10)
app/api/finans/masraf-talebi/[id]/route.ts  (yeni — Task 10)
app/finans/masraf-talebi/page.tsx           (yeni — Task 10)

components/Nav.tsx                          (değişiklik — Task 11)
```

---

### Task 1: Migration — Faz 1 tabloları

**Files:**
- Create: `migrations/076_finans_temel_tanimlar.sql`

**Interfaces:**
- Produces: aşağıdaki tablolar ve kolon adları — sonraki tüm task'lar bu şemayı
  birebir kullanır.

- [ ] **Step 1: Migration dosyasını yaz**

```sql
-- Migration: 076_finans_temel_tanimlar
-- Finans modülü Faz 1: hesap planı, kategori, masraf merkezi, proje, kasa/banka,
-- para birimi/kur, vergi kodu, ödeme yöntemi, belge türü, gelir-gider, masraf talebi.

CREATE TABLE IF NOT EXISTS finans_hesap_plani (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  kod VARCHAR(20) NOT NULL UNIQUE,
  ad VARCHAR(200) NOT NULL,
  ust_hesap_id VARCHAR(36) NULL,
  tip ENUM('varlik','borc','ozkaynak','gelir','gider') NOT NULL,
  is_active TINYINT(1) NOT NULL DEFAULT 1,
  created_by VARCHAR(36),
  created_at VARCHAR(30) NOT NULL,
  updated_at VARCHAR(30) NOT NULL,
  INDEX idx_hesap_ust (ust_hesap_id),
  FOREIGN KEY (ust_hesap_id) REFERENCES finans_hesap_plani(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS finans_kategori (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  ad VARCHAR(200) NOT NULL,
  tip ENUM('gelir','gider') NOT NULL,
  hesap_id VARCHAR(36) NULL,
  is_active TINYINT(1) NOT NULL DEFAULT 1,
  created_by VARCHAR(36),
  created_at VARCHAR(30) NOT NULL,
  updated_at VARCHAR(30) NOT NULL,
  INDEX idx_kategori_hesap (hesap_id),
  FOREIGN KEY (hesap_id) REFERENCES finans_hesap_plani(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS finans_masraf_merkezi (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  ad VARCHAR(200) NOT NULL,
  company_id VARCHAR(36) NULL,
  is_active TINYINT(1) NOT NULL DEFAULT 1,
  created_by VARCHAR(36),
  created_at VARCHAR(30) NOT NULL,
  updated_at VARCHAR(30) NOT NULL,
  INDEX idx_masraf_merkezi_company (company_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS finans_proje (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  ad VARCHAR(200) NOT NULL,
  kod VARCHAR(50),
  company_id VARCHAR(36) NULL,
  baslangic_tarihi DATE NULL,
  bitis_tarihi DATE NULL,
  durum ENUM('planlanan','aktif','tamamlandi','iptal') NOT NULL DEFAULT 'aktif',
  is_active TINYINT(1) NOT NULL DEFAULT 1,
  created_by VARCHAR(36),
  created_at VARCHAR(30) NOT NULL,
  updated_at VARCHAR(30) NOT NULL,
  INDEX idx_proje_company (company_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS finans_para_birimi (
  kod VARCHAR(10) NOT NULL PRIMARY KEY,
  ad VARCHAR(100) NOT NULL,
  is_active TINYINT(1) NOT NULL DEFAULT 1,
  created_at VARCHAR(30) NOT NULL,
  updated_at VARCHAR(30) NOT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

INSERT IGNORE INTO finans_para_birimi (kod, ad, is_active, created_at, updated_at)
VALUES
  ('TRY', 'Türk Lirası', 1, '2026-07-21T00:00:00.000Z', '2026-07-21T00:00:00.000Z'),
  ('USD', 'Amerikan Doları', 1, '2026-07-21T00:00:00.000Z', '2026-07-21T00:00:00.000Z'),
  ('EUR', 'Euro', 1, '2026-07-21T00:00:00.000Z', '2026-07-21T00:00:00.000Z');

-- finans_kur (günlük resmi kur tablosu) bilinçli olarak Faz 1'e dahil edilmedi —
-- hiçbir Faz 1 task'ı onu okumuyor/yazmıyor (ölü şema olurdu). Her finans_gelir_gider
-- satırı zaten kendi `kur` alanını taşıyor (işlem anında elle girilir); günlük resmi
-- kur tablosu ve kur farkı hesaplaması Faz 3'te (tam muhasebe/kur farkı) eklenecek.

CREATE TABLE IF NOT EXISTS finans_kasa_banka_hesabi (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  ad VARCHAR(200) NOT NULL,
  tip ENUM('kasa','banka','kredi_karti','pos') NOT NULL,
  banka_adi VARCHAR(200) NULL,
  iban VARCHAR(50) NULL,
  para_birimi_kod VARCHAR(10) NOT NULL DEFAULT 'TRY',
  acilis_bakiyesi DECIMAL(14,2) NOT NULL DEFAULT 0,
  company_id VARCHAR(36) NULL,
  is_active TINYINT(1) NOT NULL DEFAULT 1,
  created_by VARCHAR(36),
  created_at VARCHAR(30) NOT NULL,
  updated_at VARCHAR(30) NOT NULL,
  INDEX idx_kasa_banka_company (company_id),
  FOREIGN KEY (para_birimi_kod) REFERENCES finans_para_birimi(kod)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS finans_vergi_kodu (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  ad VARCHAR(100) NOT NULL,
  oran DECIMAL(5,2) NOT NULL,
  gecerlilik_baslangic DATE NOT NULL,
  gecerlilik_bitis DATE NULL,
  is_active TINYINT(1) NOT NULL DEFAULT 1,
  created_by VARCHAR(36),
  created_at VARCHAR(30) NOT NULL,
  updated_at VARCHAR(30) NOT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS finans_odeme_yontemi (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  ad VARCHAR(100) NOT NULL,
  is_active TINYINT(1) NOT NULL DEFAULT 1,
  created_by VARCHAR(36),
  created_at VARCHAR(30) NOT NULL,
  updated_at VARCHAR(30) NOT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS finans_gelir_gider (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  tur ENUM('gelir','gider') NOT NULL,
  belge_tarihi DATE NOT NULL,
  kayit_tarihi VARCHAR(30) NOT NULL,
  tahakkuk_tarihi DATE NULL,
  vade_tarihi DATE NULL,
  cari_tip ENUM('musteri','tedarikci') NULL,
  cari_id VARCHAR(36) NULL,
  kategori_id VARCHAR(36) NULL,
  net_tutar DECIMAL(14,2) NOT NULL DEFAULT 0,
  vergi_tutari DECIMAL(14,2) NOT NULL DEFAULT 0,
  brut_tutar DECIMAL(14,2) NOT NULL DEFAULT 0,
  para_birimi_kod VARCHAR(10) NOT NULL DEFAULT 'TRY',
  kur DECIMAL(12,6) NOT NULL DEFAULT 1,
  company_id VARCHAR(36) NULL,
  department_id VARCHAR(36) NULL,
  proje_id VARCHAR(36) NULL,
  masraf_merkezi_id VARCHAR(36) NULL,
  odeme_durumu ENUM('odenmedi','kismen_odendi','odendi','fazla_odendi') NOT NULL DEFAULT 'odenmedi',
  durum ENUM('taslak','onay_bekliyor','onaylandi','reddedildi') NOT NULL DEFAULT 'taslak',
  aciklama TEXT,
  etiketler TEXT,
  tekrarlama_json TEXT,
  created_by VARCHAR(36) NOT NULL,
  approved_by VARCHAR(36) NULL,
  approved_at VARCHAR(30) NULL,
  created_at VARCHAR(30) NOT NULL,
  updated_at VARCHAR(30) NOT NULL,
  INDEX idx_gg_tur_durum (tur, durum),
  INDEX idx_gg_belge_tarihi (belge_tarihi),
  INDEX idx_gg_vade_tarihi (vade_tarihi),
  INDEX idx_gg_company (company_id),
  INDEX idx_gg_kategori (kategori_id),
  FOREIGN KEY (kategori_id) REFERENCES finans_kategori(id) ON DELETE SET NULL,
  FOREIGN KEY (department_id) REFERENCES departments(id) ON DELETE SET NULL,
  FOREIGN KEY (proje_id) REFERENCES finans_proje(id) ON DELETE SET NULL,
  FOREIGN KEY (masraf_merkezi_id) REFERENCES finans_masraf_merkezi(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS finans_masraf_talebi (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  talep_eden_user_id VARCHAR(36) NOT NULL,
  tarih DATE NOT NULL,
  baslik VARCHAR(300) NOT NULL,
  aciklama TEXT,
  tahmini_tutar DECIMAL(14,2) NOT NULL,
  para_birimi_kod VARCHAR(10) NOT NULL DEFAULT 'TRY',
  kategori_id VARCHAR(36) NULL,
  department_id VARCHAR(36) NULL,
  proje_id VARCHAR(36) NULL,
  masraf_merkezi_id VARCHAR(36) NULL,
  durum ENUM('bekliyor','onaylandi','reddedildi','tamamlandi') NOT NULL DEFAULT 'bekliyor',
  onaylayan_user_id VARCHAR(36) NULL,
  onay_tarihi VARCHAR(30) NULL,
  red_nedeni VARCHAR(500) NULL,
  iliskili_gelir_gider_id VARCHAR(36) NULL,
  created_at VARCHAR(30) NOT NULL,
  updated_at VARCHAR(30) NOT NULL,
  INDEX idx_mt_talep_eden (talep_eden_user_id),
  INDEX idx_mt_durum (durum),
  FOREIGN KEY (talep_eden_user_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (kategori_id) REFERENCES finans_kategori(id) ON DELETE SET NULL,
  FOREIGN KEY (department_id) REFERENCES departments(id) ON DELETE SET NULL,
  FOREIGN KEY (proje_id) REFERENCES finans_proje(id) ON DELETE SET NULL,
  FOREIGN KEY (masraf_merkezi_id) REFERENCES finans_masraf_merkezi(id) ON DELETE SET NULL,
  FOREIGN KEY (iliskili_gelir_gider_id) REFERENCES finans_gelir_gider(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

- [ ] **Step 2: Tünel ve dev sunucusunu ayağa kaldır**

```bash
nc -z -w3 127.0.0.1 3307 || (pkill -f "ssh.*3307"; sshpass -p 'kayra123' ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -o ServerAliveCountMax=3 -f -N -L 3307:172.22.0.3:3306 root@212.64.201.150)
pkill -f "next dev"; sleep 1
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && nohup npm run dev > /tmp/aycanops-dev.log 2>&1 &
sleep 8
```

- [ ] **Step 3: Migration'ın uygulandığını doğrula**

```bash
grep "076_finans_temel_tanimlar" /tmp/aycanops-dev.log
```
Beklenen: `Migration 076_finans_temel_tanimlar.sql applied successfully` satırı, hata yok.

- [ ] **Step 4: Tabloların gerçekten oluştuğunu DB'den doğrula**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node -e "
const mysql = require('mysql2/promise');
(async () => {
  const conn = await mysql.createConnection({ host: '127.0.0.1', port: 3307, user: 'aycanops', password: 'Kayra2190.', database: 'aycanops_db' });
  const [rows] = await conn.query(\"SHOW TABLES LIKE 'finans_%'\");
  console.log(rows);
  const [pb] = await conn.query('SELECT kod FROM finans_para_birimi');
  console.log(pb);
  await conn.end();
})().catch(e => { console.error(e.message); process.exit(1); });
"
```
Beklenen: 10 `finans_%` tablosu listelenir, `finans_para_birimi`'de TRY/USD/EUR 3 satır.

- [ ] **Step 5: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add migrations/076_finans_temel_tanimlar.sql
git commit -m "Finans Faz 1: temel tanım + gelir-gider + masraf talebi tabloları"
```

---

### Task 2: Yetkilendirme — `lib/permissions.ts`

**Files:**
- Modify: `lib/permissions.ts`

**Interfaces:**
- Consumes: `PERMISSIONS` kataloğu, `DEFAULT_ROLE_PERMISSIONS.yetkili`/`.yonetici`
  objelerinin mevcut yapısı (bkz. dosyanın 6-60 ve 130-436 satırları arası).
- Produces: `"finans_hesap_plani:read"` gibi `PermissionKey` string'leri — Task 4-10
  bunları `hasPermission(user, "...")` çağrılarında kullanır.

- [ ] **Step 1: `PERMISSIONS` kataloğuna finans kaynaklarını ekle**

`lib/permissions.ts` içinde `export const PERMISSIONS = {` bloğunun içine, `anketler:`
satırından hemen önce (veya kataloğun sonuna, sıra önemli değil) ekle:

```ts
  finans_hesap_plani: ["read", "create", "update", "delete"],
  finans_kategori: ["read", "create", "update", "delete"],
  finans_masraf_merkezi: ["read", "create", "update", "delete"],
  finans_proje: ["read", "create", "update", "delete"],
  finans_kasa_banka: ["read", "create", "update", "delete"],
  finans_para_birimi: ["read"],
  finans_vergi_kodu: ["read", "create", "update", "delete"],
  finans_odeme_yontemi: ["read", "create", "update", "delete"],
  finans_gelir_gider: ["read", "create", "update", "delete", "approve"],
  finans_masraf_talebi: ["read", "create", "approve", "reject"],
```

- [ ] **Step 2: `DEFAULT_ROLE_PERMISSIONS.personel`'e masraf talebi hakkı ekle**

`personel: [` dizisinin içine ekle (herkes masraf talebi açabilmeli — kullanıcının
kendi örneği: "bilgisayarıma bir şey alacaksam talep açacağım"):

```ts
    "finans_masraf_talebi:read",
    "finans_masraf_talebi:create",
```

- [ ] **Step 3: `DEFAULT_ROLE_PERMISSIONS.yetkili`'ye finans okuma ekle**

`yetkili: [` dizisinin içine ekle:

```ts
    "finans_masraf_talebi:read",
    "finans_masraf_talebi:create",
    "finans_gelir_gider:read",
    "finans_hesap_plani:read",
    "finans_kategori:read",
    "finans_masraf_merkezi:read",
    "finans_proje:read",
    "finans_kasa_banka:read",
    "finans_para_birimi:read",
    "finans_vergi_kodu:read",
    "finans_odeme_yontemi:read",
```

- [ ] **Step 4: `DEFAULT_ROLE_PERMISSIONS.yonetici`'ye tam finans yönetimi ekle**

`yonetici: [` dizisinin içine ekle:

```ts
    "finans_masraf_talebi:read",
    "finans_masraf_talebi:create",
    "finans_masraf_talebi:approve",
    "finans_masraf_talebi:reject",
    "finans_gelir_gider:read",
    "finans_gelir_gider:create",
    "finans_gelir_gider:update",
    "finans_gelir_gider:approve",
    "finans_hesap_plani:read",
    "finans_hesap_plani:create",
    "finans_hesap_plani:update",
    "finans_kategori:read",
    "finans_kategori:create",
    "finans_kategori:update",
    "finans_masraf_merkezi:read",
    "finans_masraf_merkezi:create",
    "finans_masraf_merkezi:update",
    "finans_proje:read",
    "finans_proje:create",
    "finans_proje:update",
    "finans_kasa_banka:read",
    "finans_kasa_banka:create",
    "finans_kasa_banka:update",
    "finans_para_birimi:read",
    "finans_vergi_kodu:read",
    "finans_vergi_kodu:create",
    "finans_vergi_kodu:update",
    "finans_odeme_yontemi:read",
    "finans_odeme_yontemi:create",
    "finans_odeme_yontemi:update",
```

(`admin` hiçbir şey eklemeye gerek yok — `Object.entries(PERMISSIONS).flatMap(...)`
ile otomatik tüm izinleri alır, bu dosyanın 436. satırı civarında.)

- [ ] **Step 2: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```
Beklenen: çıktı yok (sıfır hata).

- [ ] **Step 3: Mevcut sistem rollerine yeni izinleri DB'de de yükle**

Migration 075'ten beri roller/izinler DB'de tutuluyor ve `roles` tablosu zaten dolu
olduğu için `DEFAULT_ROLE_PERMISSIONS`'taki bu yeni satırlar otomatik seed edilmez —
elle eklenmesi gerekir. `/admin/roller` sayfasından "Yönetici" rolünü aç, yukarıdaki
Step 2-4'te eklenen tüm `finans_*` iznini işaretleyip kaydet (veya sayfa içinden
`fetch("/api/admin/roles/<yonetici-id>/permissions")` GET+PUT ile toplu ekle — bkz.
bu deponun aynı günkü sohbetinde `/admin/roller` üzerinden yapılan `inspections:delete`
ekleme örneği). "Personel" ve "Yetkili" rolleri için de aynı şekilde
`finans_masraf_talebi:read`/`:create` (ve Yetkili için ek finans okuma izinleri) ekle.

- [ ] **Step 4: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add lib/permissions.ts
git commit -m "Finans Faz 1: finans_* kaynaklarını izin kataloğuna ekle"
```

---

### Task 3: Zod şemaları — `lib/schemas.ts`

**Files:**
- Modify: `lib/schemas.ts`

**Interfaces:**
- Consumes: dosyanın başındaki `shortStr(n)` yardımcı fonksiyonu (zaten var, bkz.
  `routeCreateSchema` tanımının hemen üstü).
- Produces: aşağıdaki şema adları — Task 4-10'daki her POST/PUT route bunları
  `safeParse` ile kullanır.

- [ ] **Step 1: Dosyanın sonuna şemaları ekle**

```ts
// ─── Finans: Hesap Planı ─────────────────────────────────────────────
export const finansHesapPlaniSchema = z.object({
  kod: shortStr(20),
  ad: shortStr(200),
  ust_hesap_id: z.string().optional().nullable(),
  tip: z.enum(["varlik", "borc", "ozkaynak", "gelir", "gider"]),
});

// ─── Finans: Kategori ─────────────────────────────────────────────────
export const finansKategoriSchema = z.object({
  ad: shortStr(200),
  tip: z.enum(["gelir", "gider"]),
  hesap_id: z.string().optional().nullable(),
});

// ─── Finans: Masraf Merkezi ───────────────────────────────────────────
export const finansMasrafMerkeziSchema = z.object({
  ad: shortStr(200),
  company_id: z.string().optional().nullable(),
});

// ─── Finans: Proje ─────────────────────────────────────────────────────
export const finansProjeSchema = z.object({
  ad: shortStr(200),
  kod: z.string().max(50).optional().nullable(),
  company_id: z.string().optional().nullable(),
  baslangic_tarihi: z.string().optional().nullable(),
  bitis_tarihi: z.string().optional().nullable(),
  durum: z.enum(["planlanan", "aktif", "tamamlandi", "iptal"]).optional(),
});

// ─── Finans: Kasa/Banka Hesabı ─────────────────────────────────────────
export const finansKasaBankaSchema = z.object({
  ad: shortStr(200),
  tip: z.enum(["kasa", "banka", "kredi_karti", "pos"]),
  banka_adi: z.string().max(200).optional().nullable(),
  iban: z.string().max(50).optional().nullable(),
  para_birimi_kod: z.string().max(10).optional(),
  acilis_bakiyesi: z.number().optional(),
  company_id: z.string().optional().nullable(),
});

// ─── Finans: Vergi Kodu ─────────────────────────────────────────────────
export const finansVergiKoduSchema = z.object({
  ad: shortStr(100),
  oran: z.number().min(0).max(100),
  gecerlilik_baslangic: z.string().min(1),
  gecerlilik_bitis: z.string().optional().nullable(),
});

// ─── Finans: Ödeme Yöntemi ──────────────────────────────────────────────
export const finansOdemeYontemiSchema = z.object({
  ad: shortStr(100),
});

// ─── Finans: Gelir-Gider ────────────────────────────────────────────────
export const finansGelirGiderSchema = z.object({
  tur: z.enum(["gelir", "gider"]),
  belge_tarihi: z.string().min(1),
  tahakkuk_tarihi: z.string().optional().nullable(),
  vade_tarihi: z.string().optional().nullable(),
  cari_tip: z.enum(["musteri", "tedarikci"]).optional().nullable(),
  cari_id: z.string().optional().nullable(),
  kategori_id: z.string().optional().nullable(),
  net_tutar: z.number().min(0),
  vergi_tutari: z.number().min(0).optional(),
  brut_tutar: z.number().min(0),
  para_birimi_kod: z.string().max(10).optional(),
  kur: z.number().optional(),
  company_id: z.string().optional().nullable(),
  department_id: z.string().optional().nullable(),
  proje_id: z.string().optional().nullable(),
  masraf_merkezi_id: z.string().optional().nullable(),
  aciklama: z.string().max(2000).optional().nullable(),
  etiketler: z.array(z.string()).optional().nullable(),
});

// ─── Finans: Masraf Talebi ──────────────────────────────────────────────
export const finansMasrafTalebiSchema = z.object({
  tarih: z.string().min(1),
  baslik: shortStr(300),
  aciklama: z.string().max(2000).optional().nullable(),
  tahmini_tutar: z.number().min(0),
  para_birimi_kod: z.string().max(10).optional(),
  kategori_id: z.string().optional().nullable(),
  department_id: z.string().optional().nullable(),
  proje_id: z.string().optional().nullable(),
  masraf_merkezi_id: z.string().optional().nullable(),
});
```

- [ ] **Step 2: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```
Beklenen: sıfır hata.

- [ ] **Step 3: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add lib/schemas.ts
git commit -m "Finans Faz 1: gelir-gider ve tanım tabloları için Zod şemaları"
```

---

### Task 4: Hesap Planı (API + sayfa)

**Files:**
- Create: `app/api/finans/hesap-plani/route.ts`
- Create: `app/api/finans/hesap-plani/[id]/route.ts`
- Create: `app/admin/finans/hesap-plani/page.tsx`

**Interfaces:**
- Consumes: `finansHesapPlaniSchema` (Task 3), `PERMISSIONS.finans_hesap_plani`
  (Task 2), `finans_hesap_plani` tablosu (Task 1).
- Produces: `GET /api/finans/hesap-plani` → `{ ok, data: [{id, kod, ad, ust_hesap_id,
  tip, is_active, ...}] }` — Task 5 (kategori formu) bu listeyi hesap seçim
  dropdown'unda kullanır.

- [ ] **Step 1: List+create route'unu yaz**

`app/api/finans/hesap-plani/route.ts`:

```ts
import { NextRequest, NextResponse } from "next/server";
import { getDb } from "@/lib/db";
import { requireUser } from "@/lib/auth";
import { hasPermission } from "@/lib/permissions";
import { v4 as uuidv4 } from "uuid";
import { nowIso } from "@/lib/time";
import { apiError } from "@/lib/api-error";
import { finansHesapPlaniSchema } from "@/lib/schemas";

export async function GET(req: NextRequest) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });
    if (!hasPermission(user, "finans_hesap_plani:read"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });

    const { searchParams } = new URL(req.url);
    const isActive = searchParams.get("is_active");
    const conditions: string[] = [];
    const params: unknown[] = [];
    if (isActive !== null && isActive !== "") {
      conditions.push("is_active = ?");
      params.push(isActive === "1" ? 1 : 0);
    }
    const where = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";

    const rows = await getDb().prepare(
      `SELECT * FROM finans_hesap_plani ${where} ORDER BY kod ASC`
    ).all(...params);
    return NextResponse.json({ ok: true, data: rows });
  } catch (e) { return apiError(e); }
}

export async function POST(req: NextRequest) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });
    if (!hasPermission(user, "finans_hesap_plani:create"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });

    const raw = await req.json();
    const parsed = finansHesapPlaniSchema.safeParse(raw);
    if (!parsed.success) return NextResponse.json({ ok: false, error: parsed.error.flatten().fieldErrors }, { status: 400 });
    const { kod, ad, ust_hesap_id, tip } = parsed.data;

    const db = getDb();
    const id = uuidv4();
    const now = nowIso();
    await db.prepare(
      `INSERT INTO finans_hesap_plani (id, kod, ad, ust_hesap_id, tip, is_active, created_by, created_at, updated_at)
       VALUES (?, ?, ?, ?, ?, 1, ?, ?, ?)`
    ).run(id, kod, ad, ust_hesap_id || null, tip, user.id, now, now);

    return NextResponse.json({ ok: true, data: { id } }, { status: 201 });
  } catch (e) { return apiError(e); }
}
```

- [ ] **Step 2: Update+delete route'unu yaz**

`app/api/finans/hesap-plani/[id]/route.ts`:

```ts
import { NextRequest, NextResponse } from "next/server";
import { getDb } from "@/lib/db";
import { requireUser } from "@/lib/auth";
import { hasPermission } from "@/lib/permissions";
import { nowIso } from "@/lib/time";
import { apiError } from "@/lib/api-error";
import { finansHesapPlaniSchema } from "@/lib/schemas";

export async function PUT(req: NextRequest, { params }: { params: Promise<{ id: string }> }) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });
    if (!hasPermission(user, "finans_hesap_plani:update"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });

    const { id } = await params;
    const raw = await req.json();

    if (raw.action === "durum-degistir") {
      await getDb().prepare(`UPDATE finans_hesap_plani SET is_active = ?, updated_at = ? WHERE id = ?`)
        .run(raw.is_active ? 1 : 0, nowIso(), id);
      return NextResponse.json({ ok: true });
    }

    const parsed = finansHesapPlaniSchema.safeParse(raw);
    if (!parsed.success) return NextResponse.json({ ok: false, error: parsed.error.flatten().fieldErrors }, { status: 400 });
    const { kod, ad, ust_hesap_id, tip } = parsed.data;

    const db = getDb();
    const existing = await db.prepare(`SELECT id FROM finans_hesap_plani WHERE id = ?`).get(id);
    if (!existing) return NextResponse.json({ ok: false, error: "Bulunamadı" }, { status: 404 });

    await db.prepare(
      `UPDATE finans_hesap_plani SET kod = ?, ad = ?, ust_hesap_id = ?, tip = ?, updated_at = ? WHERE id = ?`
    ).run(kod, ad, ust_hesap_id || null, tip, nowIso(), id);

    return NextResponse.json({ ok: true });
  } catch (e) { return apiError(e); }
}

export async function DELETE(_req: NextRequest, { params }: { params: Promise<{ id: string }> }) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });
    if (!hasPermission(user, "finans_hesap_plani:delete"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });

    const { id } = await params;
    await getDb().prepare(`DELETE FROM finans_hesap_plani WHERE id = ?`).run(id);
    return NextResponse.json({ ok: true });
  } catch (e) { return apiError(e); }
}
```

- [ ] **Step 3: Sayfayı yaz**

`app/admin/finans/hesap-plani/page.tsx` — `app/admin/arac-gruplari/page.tsx`'in
(bkz. mevcut dosya) birebir aynı iskeleti, aşağıdaki farklarla:

```tsx
"use client";
import { useState, useEffect } from "react";
import { useRouter } from "next/navigation";
import Nav from "@/components/Nav";
import { toast } from "@/lib/toast";
import { hasPermission } from "@/lib/permissions";

const TIP_LABELS: Record<string, string> = {
  varlik: "Varlık", borc: "Borç", ozkaynak: "Özkaynak", gelir: "Gelir", gider: "Gider",
};
const EMPTY_FORM = { kod: "", ad: "", ust_hesap_id: "", tip: "gider" };

export default function HesapPlaniPage() {
  const router = useRouter();
  const [user, setUser] = useState<any>(null);
  const [rows, setRows] = useState<any[]>([]);
  const [loading, setLoading] = useState(true);
  const [showForm, setShowForm] = useState(false);
  const [editing, setEditing] = useState<any>(null);
  const [form, setForm] = useState({ ...EMPTY_FORM });
  const [saving, setSaving] = useState(false);
  const [saveError, setSaveError] = useState<string | null>(null);

  useEffect(() => {
    fetch("/api/auth/me").then(r => r.json()).then(d => {
      if (d.ok) setUser(d.data); else router.replace("/login");
    });
    load();
  }, []);

  async function load() {
    setLoading(true);
    try {
      const r = await fetch("/api/finans/hesap-plani");
      const d = await r.json();
      if (d.ok) setRows(d.data);
    } finally { setLoading(false); }
  }

  function openCreate() { setEditing(null); setForm({ ...EMPTY_FORM }); setSaveError(null); setShowForm(true); }
  function openEdit(row: any) {
    setEditing(row);
    setForm({ kod: row.kod, ad: row.ad, ust_hesap_id: row.ust_hesap_id || "", tip: row.tip });
    setSaveError(null); setShowForm(true);
  }

  async function save() {
    if (!form.kod.trim() || !form.ad.trim()) { setSaveError("Kod ve ad zorunludur"); return; }
    setSaving(true); setSaveError(null);
    try {
      const url = editing ? `/api/finans/hesap-plani/${editing.id}` : "/api/finans/hesap-plani";
      const method = editing ? "PUT" : "POST";
      const res = await fetch(url, { method, headers: { "Content-Type": "application/json" }, body: JSON.stringify(form) });
      const d = await res.json();
      if (!d.ok) { setSaveError(typeof d.error === "string" ? d.error : "Kayıt hatası"); return; }
      toast.success(editing ? "Hesap güncellendi" : "Hesap eklendi");
      setShowForm(false);
      load();
    } finally { setSaving(false); }
  }

  const canCreate = hasPermission(user, "finans_hesap_plani:create");

  return (
    <>
      <Nav user={user} />
      <div className="min-h-screen bg-zinc-950 pt-16">
        <div className="max-w-3xl mx-auto px-4 py-6">
          <div className="flex items-center justify-between gap-4 mb-6">
            <div>
              <h1 className="text-2xl font-bold text-white">Hesap Planı</h1>
              <p className="text-zinc-500 text-sm mt-0.5">{rows.length} hesap</p>
            </div>
            {canCreate && (
              <button onClick={openCreate} className="bg-white text-zinc-950 font-semibold text-sm px-4 py-2 rounded-xl hover:bg-zinc-200 transition-colors whitespace-nowrap">
                + Yeni Hesap
              </button>
            )}
          </div>

          {loading ? (
            <div className="space-y-2">{Array.from({ length: 3 }).map((_, i) => <div key={i} className="bg-zinc-900 border border-zinc-800 rounded-xl h-16 animate-pulse" />)}</div>
          ) : rows.length === 0 ? (
            <div className="text-center py-16 text-zinc-600">Henüz hesap tanımlanmadı</div>
          ) : (
            <div className="space-y-2">
              {rows.map(row => (
                <div key={row.id} onClick={() => openEdit(row)} className="bg-zinc-900 border border-zinc-800 rounded-xl px-4 py-3 cursor-pointer hover:border-zinc-700 transition-colors">
                  <div className="flex items-center gap-2 flex-wrap">
                    <span className="font-mono text-zinc-500 text-xs">{row.kod}</span>
                    <span className="text-white font-semibold text-sm">{row.ad}</span>
                    <span className="text-zinc-500 text-xs ml-auto">{TIP_LABELS[row.tip]}</span>
                  </div>
                </div>
              ))}
            </div>
          )}
        </div>
      </div>

      {showForm && (
        <div className="fixed inset-0 z-50 flex">
          <div className="flex-1 bg-black/60" onClick={() => setShowForm(false)} />
          <div className="w-full max-w-md bg-zinc-900 border-l border-zinc-800 flex flex-col overflow-hidden">
            <div className="flex items-center justify-between px-5 py-4 border-b border-zinc-800 shrink-0">
              <h2 className="text-white font-semibold">{editing ? "Hesabı Düzenle" : "Yeni Hesap"}</h2>
              <button onClick={() => setShowForm(false)} className="text-zinc-500 hover:text-white text-xl leading-none">×</button>
            </div>
            <div className="flex-1 overflow-y-auto p-5 space-y-4">
              {saveError && <div className="bg-red-950 border border-red-800 text-red-300 text-sm px-3 py-2 rounded-lg">{saveError}</div>}
              <label className="block">
                <span className="text-zinc-400 text-xs font-medium mb-1 block">Hesap Kodu *</span>
                <input value={form.kod} onChange={e => setForm(f => ({ ...f, kod: e.target.value }))}
                  placeholder="örn. 770"
                  className="w-full bg-zinc-800 border border-zinc-700 text-white text-sm px-3 py-2 rounded-lg focus:outline-none focus:border-zinc-500" />
              </label>
              <label className="block">
                <span className="text-zinc-400 text-xs font-medium mb-1 block">Hesap Adı *</span>
                <input value={form.ad} onChange={e => setForm(f => ({ ...f, ad: e.target.value }))}
                  placeholder="örn. Genel Yönetim Giderleri"
                  className="w-full bg-zinc-800 border border-zinc-700 text-white text-sm px-3 py-2 rounded-lg focus:outline-none focus:border-zinc-500" />
              </label>
              <label className="block">
                <span className="text-zinc-400 text-xs font-medium mb-1 block">Tip *</span>
                <select value={form.tip} onChange={e => setForm(f => ({ ...f, tip: e.target.value }))}
                  className="w-full bg-zinc-800 border border-zinc-700 text-white text-sm px-3 py-2 rounded-lg focus:outline-none">
                  {Object.entries(TIP_LABELS).map(([v, l]) => <option key={v} value={v}>{l}</option>)}
                </select>
              </label>
            </div>
            <div className="px-5 py-4 border-t border-zinc-800 flex gap-3 shrink-0">
              <button onClick={() => setShowForm(false)} className="flex-1 bg-zinc-800 text-zinc-300 font-medium text-sm py-2.5 rounded-xl hover:bg-zinc-700 transition-colors">İptal</button>
              <button onClick={save} disabled={saving || !form.kod.trim() || !form.ad.trim()}
                className="flex-1 bg-white text-zinc-950 font-semibold text-sm py-2.5 rounded-xl hover:bg-zinc-200 disabled:opacity-50 transition-colors">
                {saving ? "Kaydediliyor..." : "Kaydet"}
              </button>
            </div>
          </div>
        </div>
      )}
    </>
  );
}
```

- [ ] **Step 4: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```
Beklenen: sıfır hata.

- [ ] **Step 5: Canlı doğrulama**

Dev sunucusu ayaktayken tarayıcıda `/admin/finans/hesap-plani`'ye admin olarak git,
"+ Yeni Hesap" ile kod=`770`, ad=`Genel Yönetim Giderleri`, tip=`Gider` gir, kaydet.
Listede görünmeli. Sonra DB'den doğrula:

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node -e "
const mysql = require('mysql2/promise');
(async () => {
  const conn = await mysql.createConnection({ host: '127.0.0.1', port: 3307, user: 'aycanops', password: 'Kayra2190.', database: 'aycanops_db' });
  const [rows] = await conn.query('SELECT kod, ad, tip FROM finans_hesap_plani');
  console.log(rows);
  await conn.end();
})().catch(e => { console.error(e.message); process.exit(1); });
"
```
Beklenen: az önce eklenen `770 / Genel Yönetim Giderleri / gider` satırı görünür.

- [ ] **Step 6: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add app/api/finans/hesap-plani app/admin/finans/hesap-plani
git commit -m "Finans Faz 1: Hesap Planı API + sayfa"
```

---

### Task 5: Kategori (API + sayfa)

**Files:**
- Create: `app/api/finans/kategori/route.ts`
- Create: `app/api/finans/kategori/[id]/route.ts`
- Create: `app/admin/finans/kategori/page.tsx`

**Interfaces:**
- Consumes: `finansKategoriSchema` (Task 3), `GET /api/finans/hesap-plani` (Task 4,
  form içindeki hesap seçim dropdown'u için).
- Produces: `GET /api/finans/kategori` → `{ ok, data: [{id, ad, tip, hesap_id, ...}] }`
  — Task 9 (gelir-gider formu) bu listeyi kategori seçim dropdown'unda kullanır.

- [ ] **Step 1: List+create route'unu yaz** (Task 4 Step 1'in birebir aynısı, sadece
  tablo/şema adları değişir)

`app/api/finans/kategori/route.ts` — `app/api/finans/hesap-plani/route.ts`'i temel al,
şu değişikliklerle: `finans_hesap_plani` → `finans_kategori`, `finansHesapPlaniSchema`
→ `finansKategoriSchema`, `finans_hesap_plani:*` → `finans_kategori:*`, INSERT
alanları `(id, ad, tip, hesap_id, is_active, created_by, created_at, updated_at)`,
`ORDER BY ad ASC`.

- [ ] **Step 2: Update+delete route'unu yaz** (Task 4 Step 2'nin aynısı, tablo/şema
  adları değişir)

- [ ] **Step 3: Sayfayı yaz**

`app/admin/finans/kategori/page.tsx` — Task 4 Step 3'teki sayfanın aynısı, farklar:
- Liste yüklerken `/api/finans/hesap-plani`'yi de çek (`accounts` state), her
  kategori satırında `accounts.find(a => a.id === row.hesap_id)?.ad` göster.
- Form alanları: `ad` (metin), `tip` (gelir/gider select), `hesap_id` (hesap planı
  select, "— Seçilmedi —" opsiyonu ile).
- Başlık: "Gelir/Gider Kategorileri".

- [ ] **Step 4: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```

- [ ] **Step 5: Canlı doğrulama**

`/admin/finans/kategori`'de "Yakıt Gideri" adında, tip=gider, hesap=770 (Task 4'te
eklenen) seçili bir kategori oluştur. Kaydedince listede "Yakıt Gideri" ve altında
hesap adı görünmeli.

- [ ] **Step 6: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add app/api/finans/kategori app/admin/finans/kategori
git commit -m "Finans Faz 1: Kategori API + sayfa"
```

---

### Task 6: Masraf Merkezi + Proje (API + sayfa)

**Files:**
- Create: `app/api/finans/masraf-merkezi/route.ts`
- Create: `app/api/finans/masraf-merkezi/[id]/route.ts`
- Create: `app/admin/finans/masraf-merkezi/page.tsx`
- Create: `app/api/finans/proje/route.ts`
- Create: `app/api/finans/proje/[id]/route.ts`
- Create: `app/admin/finans/proje/page.tsx`

**Interfaces:**
- Consumes: `finansMasrafMerkeziSchema`, `finansProjeSchema` (Task 3),
  `GET /api/companies` (mevcut endpoint, firma seçim dropdown'u için).
- Produces: `GET /api/finans/masraf-merkezi`, `GET /api/finans/proje` — Task 9 ve
  Task 10'daki formlar bu listeleri kullanır.

- [ ] **Step 1: Masraf merkezi route'larını yaz** (Task 4 Step 1-2 deseni,
  `finans_masraf_merkezi` tablosu, alanlar `ad`, `company_id`)

- [ ] **Step 2: Masraf merkezi sayfasını yaz** — Task 4 Step 3 deseni, form alanları:
  `ad` (metin), `company_id` (firma seçim dropdown, `/api/companies`'ten). Başlık:
  "Masraf Merkezleri".

- [ ] **Step 3: Proje route'larını yaz** (Task 4 Step 1-2 deseni, `finans_proje`
  tablosu, alanlar `ad`, `kod`, `company_id`, `baslangic_tarihi`, `bitis_tarihi`,
  `durum`)

- [ ] **Step 4: Proje sayfasını yaz** — form alanları: `ad`, `kod`, `company_id`
  (dropdown), `baslangic_tarihi`/`bitis_tarihi` (`type="date"` input), `durum`
  (planlanan/aktif/tamamlandi/iptal select). Başlık: "Projeler / İş Emirleri".

- [ ] **Step 5: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```

- [ ] **Step 6: Canlı doğrulama**

`/admin/finans/masraf-merkezi`'de "Genel Merkez" masraf merkezi oluştur.
`/admin/finans/proje`'de "Ofis Yenileme" adında, durum=aktif bir proje oluştur.
İkisi de listede görünmeli.

- [ ] **Step 7: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add app/api/finans/masraf-merkezi app/admin/finans/masraf-merkezi app/api/finans/proje app/admin/finans/proje
git commit -m "Finans Faz 1: Masraf Merkezi + Proje API + sayfa"
```

---

### Task 7: Kasa/Banka Hesabı (API + sayfa)

**Files:**
- Create: `app/api/finans/kasa-banka/route.ts`
- Create: `app/api/finans/kasa-banka/[id]/route.ts`
- Create: `app/admin/finans/kasa-banka/page.tsx`

**Interfaces:**
- Consumes: `finansKasaBankaSchema` (Task 3). Para birimi seçimi için ayrı bir API
  yok — Task 1'de `finans_para_birimi` tablosuna seed edilen `TRY`/`USD`/`EUR` sabit
  olduğu için sayfada hardcoded `["TRY","USD","EUR"]` select kullanılır.
- Produces: `GET /api/finans/kasa-banka` → Task 9'daki gelir-gider formunda "hangi
  hesaba" seçimi için kullanılabilir (opsiyonel alan, bu fazda zorunlu değil).

- [ ] **Step 1: Route'ları yaz** (Task 4 deseni, `finans_kasa_banka_hesabi` tablosu,
  alanlar `ad`, `tip`, `banka_adi`, `iban`, `para_birimi_kod`, `acilis_bakiyesi`,
  `company_id`; kaynak adı `finans_kasa_banka`)

- [ ] **Step 2: Sayfayı yaz** — form alanları: `ad`, `tip` (kasa/banka/kredi_karti/pos
  select), `banka_adi` (tip=banka ise göster), `iban`, `para_birimi_kod` (TRY/USD/EUR
  select), `acilis_bakiyesi` (number input), `company_id` (dropdown, opsiyonel —
  "— Genel (firma bağımsız) —" seçeneği ile). Başlık: "Kasa / Banka Hesapları".

- [ ] **Step 3: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```

- [ ] **Step 4: Canlı doğrulama**

`/admin/finans/kasa-banka`'da "Ana Kasa" (tip=kasa, TRY, açılış bakiyesi=0) ve
"İş Bankası Vadesiz" (tip=banka, TRY) kayıtları oluştur, listede görünmeli.

- [ ] **Step 5: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add app/api/finans/kasa-banka app/admin/finans/kasa-banka
git commit -m "Finans Faz 1: Kasa/Banka Hesabı API + sayfa"
```

---

### Task 8: Vergi Kodu + Ödeme Yöntemi (API + sayfa)

**Files:**
- Create: `app/api/finans/vergi-kodu/route.ts`
- Create: `app/api/finans/vergi-kodu/[id]/route.ts`
- Create: `app/admin/finans/vergi-kodu/page.tsx`
- Create: `app/api/finans/odeme-yontemi/route.ts`
- Create: `app/api/finans/odeme-yontemi/[id]/route.ts`
- Create: `app/admin/finans/odeme-yontemi/page.tsx`

**Interfaces:**
- Consumes: `finansVergiKoduSchema`, `finansOdemeYontemiSchema` (Task 3).
- Produces: her ikisi de bu fazda başka bir task tarafından tüketilmiyor (Faz 2'de
  fatura kalemlerinde ve ödeme kayıtlarında kullanılacak) — bağımsız tanım sayfaları.

- [ ] **Step 1: Vergi kodu route'larını yaz** (Task 4 deseni, `finans_vergi_kodu`
  tablosu, alanlar `ad`, `oran`, `gecerlilik_baslangic`, `gecerlilik_bitis`)

- [ ] **Step 2: Vergi kodu sayfasını yaz** — form alanları: `ad` (örn. "KDV %20"),
  `oran` (number, % işareti gösterimi), `gecerlilik_baslangic`/`gecerlilik_bitis`
  (`type="date"`). Başlık: "Vergi Kodları". **Önemli:** oran koda sabit gömülmedi,
  DB'den okunuyor — spec'in "tarih aralıklı, versiyonlanabilir tanımlar kullan"
  kısıtı burada karşılanıyor.

- [ ] **Step 3: Ödeme yöntemi route'larını yaz** (Task 4 deseni, en basit hâli —
  sadece `ad` alanı)

- [ ] **Step 4: Ödeme yöntemi sayfasını yaz** — tek alanlı form (`ad`, örn. "Nakit",
  "Havale/EFT", "Kredi Kartı", "Çek"). Başlık: "Ödeme Yöntemleri".

- [ ] **Step 5: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```

- [ ] **Step 6: Canlı doğrulama**

`/admin/finans/vergi-kodu`'nda "KDV %20" (oran=20, başlangıç=2026-01-01) oluştur.
`/admin/finans/odeme-yontemi`'nde "Nakit", "Havale/EFT" oluştur. İkisi de listede
görünmeli.

- [ ] **Step 7: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add app/api/finans/vergi-kodu app/admin/finans/vergi-kodu app/api/finans/odeme-yontemi app/admin/finans/odeme-yontemi
git commit -m "Finans Faz 1: Vergi Kodu + Ödeme Yöntemi API + sayfa"
```

---

### Task 9: Gelir-Gider ana modülü

**Files:**
- Create: `app/api/finans/gelir-gider/route.ts`
- Create: `app/api/finans/gelir-gider/[id]/route.ts`
- Create: `app/finans/gelir-gider/page.tsx`

**Interfaces:**
- Consumes: `finansGelirGiderSchema` (Task 3), `GET /api/finans/kategori` (Task 5),
  `GET /api/finans/masraf-merkezi`, `GET /api/finans/proje` (Task 6), `GET
  /api/departments` (mevcut), `GET /api/companies` (mevcut).
- Produces: `GET /api/finans/gelir-gider` → `{ ok, data: [...] }`, kayıtlar
  `kategori_ad`, `department_ad`, `proje_ad`, `masraf_merkezi_ad`, `company_name`
  JOIN'lenmiş olarak döner (Task 10'daki masraf talebi onayı bu tabloya taslak satır
  yazacağı için alan adları burada sabitleniyor).

- [ ] **Step 1: List+create route'unu yaz**

`app/api/finans/gelir-gider/route.ts`:

```ts
import { NextRequest, NextResponse } from "next/server";
import { getDb } from "@/lib/db";
import { requireUser } from "@/lib/auth";
import { hasPermission } from "@/lib/permissions";
import { v4 as uuidv4 } from "uuid";
import { nowIso } from "@/lib/time";
import { apiError } from "@/lib/api-error";
import { finansGelirGiderSchema } from "@/lib/schemas";

export async function GET(req: NextRequest) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });
    if (!hasPermission(user, "finans_gelir_gider:read"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });

    const { searchParams } = new URL(req.url);
    const tur = searchParams.get("tur");
    const durum = searchParams.get("durum");
    const conditions: string[] = [];
    const params: unknown[] = [];
    if (tur) { conditions.push("gg.tur = ?"); params.push(tur); }
    if (durum) { conditions.push("gg.durum = ?"); params.push(durum); }
    if (user.allowed_companies) {
      const allowed: string[] = JSON.parse(user.allowed_companies);
      if (allowed.length === 0) return NextResponse.json({ ok: true, data: [] });
      conditions.push(`(gg.company_id IS NULL OR gg.company_id IN (${allowed.map(() => "?").join(",")}))`);
      params.push(...allowed);
    }
    const where = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";

    const rows = await getDb().prepare(
      `SELECT gg.*, k.ad AS kategori_ad, d.name AS department_ad, p.ad AS proje_ad,
              mm.ad AS masraf_merkezi_ad, c.name AS company_name, u.full_name AS created_by_name
       FROM finans_gelir_gider gg
       LEFT JOIN finans_kategori k ON k.id = gg.kategori_id
       LEFT JOIN departments d ON d.id = gg.department_id
       LEFT JOIN finans_proje p ON p.id = gg.proje_id
       LEFT JOIN finans_masraf_merkezi mm ON mm.id = gg.masraf_merkezi_id
       LEFT JOIN companies c ON c.id = gg.company_id
       LEFT JOIN users u ON u.id = gg.created_by
       ${where}
       ORDER BY gg.belge_tarihi DESC, gg.kayit_tarihi DESC`
    ).all(...params);
    return NextResponse.json({ ok: true, data: rows });
  } catch (e) { return apiError(e); }
}

export async function POST(req: NextRequest) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });
    if (!hasPermission(user, "finans_gelir_gider:create"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });

    const raw = await req.json();
    const parsed = finansGelirGiderSchema.safeParse(raw);
    if (!parsed.success) return NextResponse.json({ ok: false, error: parsed.error.flatten().fieldErrors }, { status: 400 });
    const d = parsed.data;

    if (d.company_id && user.allowed_companies) {
      const allowed: string[] = JSON.parse(user.allowed_companies);
      if (!allowed.includes(d.company_id))
        return NextResponse.json({ ok: false, error: "Bu firmaya erişim yetkiniz yok" }, { status: 403 });
    }

    const db = getDb();
    const id = uuidv4();
    const now = nowIso();
    await db.prepare(
      `INSERT INTO finans_gelir_gider
         (id, tur, belge_tarihi, kayit_tarihi, tahakkuk_tarihi, vade_tarihi, cari_tip, cari_id,
          kategori_id, net_tutar, vergi_tutari, brut_tutar, para_birimi_kod, kur,
          company_id, department_id, proje_id, masraf_merkezi_id, odeme_durumu, durum,
          aciklama, etiketler, created_by, created_at, updated_at)
       VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 'odenmedi', 'taslak', ?, ?, ?, ?, ?)`
    ).run(
      id, d.tur, d.belge_tarihi, now, d.tahakkuk_tarihi || null, d.vade_tarihi || null,
      d.cari_tip || null, d.cari_id || null, d.kategori_id || null,
      d.net_tutar, d.vergi_tutari ?? 0, d.brut_tutar, d.para_birimi_kod || "TRY", d.kur ?? 1,
      d.company_id || null, d.department_id || null, d.proje_id || null, d.masraf_merkezi_id || null,
      d.aciklama || null, d.etiketler ? JSON.stringify(d.etiketler) : null,
      user.id, now, now
    );

    return NextResponse.json({ ok: true, data: { id } }, { status: 201 });
  } catch (e) { return apiError(e); }
}
```

- [ ] **Step 2: Update+delete+onay route'unu yaz**

`app/api/finans/gelir-gider/[id]/route.ts`:

```ts
import { NextRequest, NextResponse } from "next/server";
import { getDb } from "@/lib/db";
import { requireUser } from "@/lib/auth";
import { hasPermission } from "@/lib/permissions";
import { nowIso } from "@/lib/time";
import { apiError } from "@/lib/api-error";
import { finansGelirGiderSchema } from "@/lib/schemas";

export async function PUT(req: NextRequest, { params }: { params: Promise<{ id: string }> }) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });

    const { id } = await params;
    const raw = await req.json();
    const db = getDb();
    const existing = await db.prepare(`SELECT id, created_by, durum FROM finans_gelir_gider WHERE id = ?`).get(id) as
      { id: string; created_by: string; durum: string } | undefined;
    if (!existing) return NextResponse.json({ ok: false, error: "Bulunamadı" }, { status: 404 });

    if (raw.action === "onayla" || raw.action === "reddet") {
      if (!hasPermission(user, "finans_gelir_gider:approve"))
        return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });
      if (existing.created_by === user.id)
        return NextResponse.json({ ok: false, error: "Kendi kaydınızı onaylayamazsınız" }, { status: 403 });

      const yeniDurum = raw.action === "onayla" ? "onaylandi" : "reddedildi";
      const now = nowIso();
      await db.prepare(
        `UPDATE finans_gelir_gider SET durum = ?, approved_by = ?, approved_at = ?, updated_at = ? WHERE id = ?`
      ).run(yeniDurum, user.id, now, now, id);
      return NextResponse.json({ ok: true });
    }

    if (!hasPermission(user, "finans_gelir_gider:update"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });
    if (existing.durum === "onaylandi")
      return NextResponse.json({ ok: false, error: "Onaylanmış kayıt düzenlenemez" }, { status: 400 });

    const parsed = finansGelirGiderSchema.safeParse(raw);
    if (!parsed.success) return NextResponse.json({ ok: false, error: parsed.error.flatten().fieldErrors }, { status: 400 });
    const d = parsed.data;
    const now = nowIso();
    await db.prepare(
      `UPDATE finans_gelir_gider SET
         tur = ?, belge_tarihi = ?, tahakkuk_tarihi = ?, vade_tarihi = ?, cari_tip = ?, cari_id = ?,
         kategori_id = ?, net_tutar = ?, vergi_tutari = ?, brut_tutar = ?, para_birimi_kod = ?, kur = ?,
         company_id = ?, department_id = ?, proje_id = ?, masraf_merkezi_id = ?, aciklama = ?, updated_at = ?
       WHERE id = ?`
    ).run(
      d.tur, d.belge_tarihi, d.tahakkuk_tarihi || null, d.vade_tarihi || null, d.cari_tip || null, d.cari_id || null,
      d.kategori_id || null, d.net_tutar, d.vergi_tutari ?? 0, d.brut_tutar, d.para_birimi_kod || "TRY", d.kur ?? 1,
      d.company_id || null, d.department_id || null, d.proje_id || null, d.masraf_merkezi_id || null,
      d.aciklama || null, now, id
    );
    return NextResponse.json({ ok: true });
  } catch (e) { return apiError(e); }
}

export async function DELETE(_req: NextRequest, { params }: { params: Promise<{ id: string }> }) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });
    if (!hasPermission(user, "finans_gelir_gider:delete"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });

    const { id } = await params;
    const db = getDb();
    const existing = await db.prepare(`SELECT durum FROM finans_gelir_gider WHERE id = ?`).get(id) as { durum: string } | undefined;
    if (!existing) return NextResponse.json({ ok: false, error: "Bulunamadı" }, { status: 404 });
    if (existing.durum === "onaylandi")
      return NextResponse.json({ ok: false, error: "Onaylanmış kayıt silinemez" }, { status: 400 });

    await db.prepare(`DELETE FROM finans_gelir_gider WHERE id = ?`).run(id);
    return NextResponse.json({ ok: true });
  } catch (e) { return apiError(e); }
}
```

- [ ] **Step 3: Sayfayı yaz**

`app/finans/gelir-gider/page.tsx` — liste (tarihe göre sıralı, tür/durum filtreli)
+ slide-over form (Task 4'teki desenle aynı, ek olarak `tur` toggle, `kategori_id`/
`department_id`/`proje_id`/`masraf_merkezi_id`/`company_id` dropdown'ları,
`net_tutar`/`vergi_tutari`/`brut_tutar` sayısal alanlar — `brut_tutar` `net_tutar +
vergi_tutari` olarak otomatik hesaplanıp salt-okunur gösterilir). Durum rozetleri
(taslak=zinc, onay_bekliyor=amber, onaylandi=emerald, reddedildi=red). Onaylama
yetkisi olan kullanıcı için satırda "Onayla"/"Reddet" butonları (`hasPermission(user,
"finans_gelir_gider:approve")` ile gizlenir, kendi kaydında da `row.created_by ===
user.id` ise gizlenir — dört-göz kuralı UI'da da yansıtılır, gerçek kontrol server'da).

Tam dosya boyutu Task 4'teki örnekten daha büyük olacağı için, iskeleti `app/admin/
arac-gruplari/page.tsx` + `app/guzergahlar/page.tsx`'teki (ikinci dosya çoklu dropdown
ve filtre deseni için referans) birleşimi olarak yaz — state, `load()`, `save()`,
`openCreate()`/`openEdit()` fonksiyonları Task 4 ile birebir aynı iskelet, sadece form
alanları ve liste satırı içeriği genişler.

- [ ] **Step 4: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```

- [ ] **Step 5: Canlı doğrulama — gider kaydı oluştur ve onayla**

1. `/finans/gelir-gider`'de tur=gider, kategori=Yakıt Gideri (Task 5), net=100,
   vergi=20, brüt=120, belge_tarihi=bugün ile bir kayıt oluştur → durum "taslak"
   görünmeli.
2. Farklı bir yönetici/admin kullanıcıyla (veya aynı admin ile — admin kendi kaydını
   da onaylayamaz kuralını farklı bir test kullanıcısıyla doğrula) "Onayla" butonuna
   bas → durum "onaylandı" olmalı, tekrar düzenlenemez/silinemez olmalı (API 400
   dönmeli, DB'den doğrula):

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node -e "
const mysql = require('mysql2/promise');
(async () => {
  const conn = await mysql.createConnection({ host: '127.0.0.1', port: 3307, user: 'aycanops', password: 'Kayra2190.', database: 'aycanops_db' });
  const [rows] = await conn.query('SELECT tur, net_tutar, brut_tutar, durum, approved_by FROM finans_gelir_gider ORDER BY created_at DESC LIMIT 1');
  console.log(rows);
  await conn.end();
})().catch(e => { console.error(e.message); process.exit(1); });
"
```
Beklenen: `durum: 'onaylandi'`, `approved_by` dolu.

- [ ] **Step 6: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add app/api/finans/gelir-gider app/finans/gelir-gider
git commit -m "Finans Faz 1: Gelir-Gider ana modülü (API + sayfa + onay akışı)"
```

---

### Task 10: Masraf Talebi modülü

**Files:**
- Create: `app/api/finans/masraf-talebi/route.ts`
- Create: `app/api/finans/masraf-talebi/[id]/route.ts`
- Create: `app/finans/masraf-talebi/page.tsx`

**Interfaces:**
- Consumes: `finansMasrafTalebiSchema` (Task 3), `finans_gelir_gider` tablosu
  (Task 1/9 — onaylanan talep buraya taslak satır yazar), mevcut `notifications`
  tablosu/insert deseni (bkz. `app/api/inspections/route.ts`'teki bildirim ekleme
  bloğu, aynı `INSERT INTO notifications (id, user_id, title, body, link, is_read,
  created_by, created_at, updated_at)` deseni kullanılacak).
- Produces: `GET /api/finans/masraf-talebi` → kullanıcı `finans_masraf_talebi:approve`
  yetkisi yoksa sadece kendi taleplerini görür (server-side filtre); yetkisi varsa
  tüm talepleri görür.

- [ ] **Step 1: List+create route'unu yaz**

`app/api/finans/masraf-talebi/route.ts`:

```ts
import { NextRequest, NextResponse } from "next/server";
import { getDb } from "@/lib/db";
import { requireUser } from "@/lib/auth";
import { hasPermission } from "@/lib/permissions";
import { v4 as uuidv4 } from "uuid";
import { nowIso } from "@/lib/time";
import { apiError } from "@/lib/api-error";
import { finansMasrafTalebiSchema } from "@/lib/schemas";

export async function GET(req: NextRequest) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });
    if (!hasPermission(user, "finans_masraf_talebi:read"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });

    const canApprove = hasPermission(user, "finans_masraf_talebi:approve");
    const { searchParams } = new URL(req.url);
    const durum = searchParams.get("durum");

    const conditions: string[] = [];
    const params: unknown[] = [];
    if (!canApprove) { conditions.push("mt.talep_eden_user_id = ?"); params.push(user.id); }
    if (durum) { conditions.push("mt.durum = ?"); params.push(durum); }
    const where = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";

    const rows = await getDb().prepare(
      `SELECT mt.*, u.full_name AS talep_eden_ad, k.ad AS kategori_ad,
              d.name AS department_ad, p.ad AS proje_ad
       FROM finans_masraf_talebi mt
       JOIN users u ON u.id = mt.talep_eden_user_id
       LEFT JOIN finans_kategori k ON k.id = mt.kategori_id
       LEFT JOIN departments d ON d.id = mt.department_id
       LEFT JOIN finans_proje p ON p.id = mt.proje_id
       ${where}
       ORDER BY mt.created_at DESC`
    ).all(...params);
    return NextResponse.json({ ok: true, data: rows });
  } catch (e) { return apiError(e); }
}

export async function POST(req: NextRequest) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });
    if (!hasPermission(user, "finans_masraf_talebi:create"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });

    const raw = await req.json();
    const parsed = finansMasrafTalebiSchema.safeParse(raw);
    if (!parsed.success) return NextResponse.json({ ok: false, error: parsed.error.flatten().fieldErrors }, { status: 400 });
    const d = parsed.data;

    const db = getDb();
    const id = uuidv4();
    const now = nowIso();
    await db.prepare(
      `INSERT INTO finans_masraf_talebi
         (id, talep_eden_user_id, tarih, baslik, aciklama, tahmini_tutar, para_birimi_kod,
          kategori_id, department_id, proje_id, masraf_merkezi_id, durum, created_at, updated_at)
       VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 'bekliyor', ?, ?)`
    ).run(
      id, user.id, d.tarih, d.baslik, d.aciklama || null, d.tahmini_tutar, d.para_birimi_kod || "TRY",
      d.kategori_id || null, d.department_id || null, d.proje_id || null, d.masraf_merkezi_id || null,
      now, now
    );

    // Onay yetkisi olan yönetici/admin rollerine bildirim gönder.
    const approvers = await db.prepare(
      `SELECT DISTINCT u.id FROM users u
       JOIN role_permissions rp ON rp.role_name = u.role
       WHERE rp.permission_key = 'finans_masraf_talebi:approve' AND u.is_active = 1`
    ).all<{ id: string }>();
    for (const approver of approvers) {
      await db.prepare(
        `INSERT INTO notifications (id, user_id, title, body, link, is_read, created_by, created_at, updated_at)
         VALUES (?, ?, ?, ?, ?, 0, ?, ?, ?)`
      ).run(uuidv4(), approver.id, `Yeni masraf talebi: ${d.baslik}`,
        `${user.full_name} tarafından ${d.tahmini_tutar} ${d.para_birimi_kod || "TRY"} tutarında talep açıldı.`,
        "/finans/masraf-talebi", user.id, now, now);
    }

    return NextResponse.json({ ok: true, data: { id } }, { status: 201 });
  } catch (e) { return apiError(e); }
}
```

- [ ] **Step 2: Onay/red route'unu yaz**

`app/api/finans/masraf-talebi/[id]/route.ts`:

```ts
import { NextRequest, NextResponse } from "next/server";
import { getDb } from "@/lib/db";
import { requireUser } from "@/lib/auth";
import { hasPermission } from "@/lib/permissions";
import { nowIso } from "@/lib/time";
import { apiError } from "@/lib/api-error";
import { v4 as uuidv4 } from "uuid";

export async function PUT(req: NextRequest, { params }: { params: Promise<{ id: string }> }) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });

    const { id } = await params;
    const raw = await req.json();
    const db = getDb();
    const existing = await db.prepare(
      `SELECT * FROM finans_masraf_talebi WHERE id = ?`
    ).get(id) as any;
    if (!existing) return NextResponse.json({ ok: false, error: "Bulunamadı" }, { status: 404 });
    if (existing.durum !== "bekliyor")
      return NextResponse.json({ ok: false, error: "Bu talep zaten sonuçlandırılmış" }, { status: 400 });

    if (!hasPermission(user, "finans_masraf_talebi:approve"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });
    if (existing.talep_eden_user_id === user.id)
      return NextResponse.json({ ok: false, error: "Kendi talebinizi onaylayamazsınız" }, { status: 403 });

    const now = nowIso();

    if (raw.action === "reddet") {
      if (!raw.red_nedeni?.trim())
        return NextResponse.json({ ok: false, error: "Red nedeni zorunludur" }, { status: 400 });
      await db.prepare(
        `UPDATE finans_masraf_talebi SET durum = 'reddedildi', onaylayan_user_id = ?, onay_tarihi = ?, red_nedeni = ?, updated_at = ? WHERE id = ?`
      ).run(user.id, now, raw.red_nedeni.trim(), now, id);
      await db.prepare(
        `INSERT INTO notifications (id, user_id, title, body, link, is_read, created_by, created_at, updated_at)
         VALUES (?, ?, ?, ?, ?, 0, ?, ?, ?)`
      ).run(uuidv4(), existing.talep_eden_user_id, `Masraf talebiniz reddedildi: ${existing.baslik}`,
        raw.red_nedeni.trim(), "/finans/masraf-talebi", user.id, now, now);
      return NextResponse.json({ ok: true });
    }

    if (raw.action === "onayla") {
      const ggId = uuidv4();
      await db.prepare(
        `INSERT INTO finans_gelir_gider
           (id, tur, belge_tarihi, kayit_tarihi, kategori_id, net_tutar, vergi_tutari, brut_tutar,
            para_birimi_kod, department_id, proje_id, masraf_merkezi_id, odeme_durumu, durum,
            aciklama, created_by, created_at, updated_at)
         VALUES (?, 'gider', ?, ?, ?, ?, 0, ?, ?, ?, ?, ?, 'odenmedi', 'taslak', ?, ?, ?, ?)`
      ).run(
        ggId, existing.tarih, now, existing.kategori_id, existing.tahmini_tutar, existing.tahmini_tutar,
        existing.para_birimi_kod, existing.department_id, existing.proje_id, existing.masraf_merkezi_id,
        `Masraf talebinden oluşturuldu: ${existing.baslik}`, existing.talep_eden_user_id, now, now
      );
      await db.prepare(
        `UPDATE finans_masraf_talebi SET durum = 'onaylandi', onaylayan_user_id = ?, onay_tarihi = ?, iliskili_gelir_gider_id = ?, updated_at = ? WHERE id = ?`
      ).run(user.id, now, ggId, now, id);
      await db.prepare(
        `INSERT INTO notifications (id, user_id, title, body, link, is_read, created_by, created_at, updated_at)
         VALUES (?, ?, ?, ?, ?, 0, ?, ?, ?)`
      ).run(uuidv4(), existing.talep_eden_user_id, `Masraf talebiniz onaylandı: ${existing.baslik}`,
        `Talebiniz onaylandı, gider kaydı oluşturuldu.`, "/finans/gelir-gider", user.id, now, now);
      return NextResponse.json({ ok: true, data: { gelir_gider_id: ggId } });
    }

    return NextResponse.json({ ok: false, error: "Geçersiz işlem" }, { status: 400 });
  } catch (e) { return apiError(e); }
}
```

- [ ] **Step 3: Sayfayı yaz**

`app/finans/masraf-talebi/page.tsx` — Task 4'teki iskelet, farklar:
- "+ Talep Oluştur" butonu **herkese** açık (`hasPermission(user,
  "finans_masraf_talebi:create")` — personel dahil).
- Form alanları: `tarih` (`type="date"`, varsayılan bugün), `baslik`, `aciklama`
  (textarea), `tahmini_tutar` (number), `para_birimi_kod` (TRY/USD/EUR select),
  `kategori_id` (dropdown, opsiyonel), `department_id`/`proje_id`/`masraf_merkezi_id`
  (dropdown, opsiyonel).
- Liste satırında durum rozeti (bekliyor=amber, onaylandi=emerald,
  reddedildi=red, tamamlandi=zinc) ve talep eden adı.
- `hasPermission(user, "finans_masraf_talebi:approve")` true ve
  `row.talep_eden_user_id !== user.id` ise satırda "Onayla" / "Reddet" butonları;
  reddet için önce bir `prompt()` ile red nedeni al (basit form, ayrı modal
  gerekmez), sonra `PUT .../[id]` çağır.

- [ ] **Step 4: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```

- [ ] **Step 5: Canlı doğrulama — uçtan uca talep akışı**

1. Personel rolündeki bir kullanıcıyla `/finans/masraf-talebi`'de "+ Talep Oluştur" →
   başlık="Klavye alımı", tahmini_tutar=500, kaydet → durum "bekliyor" görünmeli.
2. Yönetici/admin ile aynı sayfaya gir, bu talebi gör, "Onayla" bas.
3. DB'den doğrula: talep `onaylandi`, `finans_gelir_gider`'de yeni bir `taslak`
   gider kaydı (tur=gider, net_tutar=500) oluşmuş olmalı:

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node -e "
const mysql = require('mysql2/promise');
(async () => {
  const conn = await mysql.createConnection({ host: '127.0.0.1', port: 3307, user: 'aycanops', password: 'Kayra2190.', database: 'aycanops_db' });
  const [mt] = await conn.query(\"SELECT baslik, durum, iliskili_gelir_gider_id FROM finans_masraf_talebi WHERE baslik = 'Klavye alımı'\");
  console.log('talep:', mt);
  if (mt[0]?.iliskili_gelir_gider_id) {
    const [gg] = await conn.query('SELECT tur, net_tutar, durum, aciklama FROM finans_gelir_gider WHERE id = ?', [mt[0].iliskili_gelir_gider_id]);
    console.log('gider kaydı:', gg);
  }
  await conn.end();
})().catch(e => { console.error(e.message); process.exit(1); });
"
```
Beklenen: talep `durum: 'onaylandi'`, ilişkili gider kaydı `tur: 'gider', net_tutar:
500, durum: 'taslak'`.

4. Personel kullanıcısının bildirimlerinde "Masraf talebiniz onaylandı: Klavye
   alımı" bildirimi görünmeli (`/api/notifications` veya UI'daki zil ikonu).

- [ ] **Step 6: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add app/api/finans/masraf-talebi app/finans/masraf-talebi
git commit -m "Finans Faz 1: Masraf Talebi modülü (talep + onay/red + gider dönüşümü + bildirim)"
```

---

### Task 11: Nav entegrasyonu

**Files:**
- Modify: `components/Nav.tsx`

**Interfaces:**
- Consumes: mevcut `NAV_PERMISSION_BY_HREF`, `YONETIM_LINKS`/yeni `FINANS_LINKS`
  dizisi deseni (bkz. dosyanın üst kısmındaki `BUGUN_LINKS`, `ARACLAR_LINKS` vb.
  tanımları ve alt kısımdaki `filterByAllowed(...)` kullanımları).

- [ ] **Step 1: Yeni link grubu tanımla**

`components/Nav.tsx`'in üst kısmındaki link grubu tanımlarının yanına (örn.
`FINANS_LINKS` adıyla, `GOREVLER_LINKS`'ten hemen sonra) ekle:

```ts
const FINANS_LINKS = [
  { href: "/finans/gelir-gider", label: "Gelir-Gider", Icon: IconCoin },
  { href: "/finans/masraf-talebi", label: "Masraf Talebi", Icon: IconClipboard2 },
];
```

(`IconCoin` ve `IconClipboard2` zaten import edilmiş — dosyanın 9-16. satırları
arasındaki import listesinde mevcut.)

- [ ] **Step 2: Grubu sidebar render bloğuna ekle**

`GOREVLER` grubunun render edildiği bloktan hemen sonra (Nav.tsx içinde `{/* GÖREVLER
*/}` yorumunu ara), aynı desenle yeni bir `{/* FİNANS */}` bloğu ekle:

```tsx
{/* FİNANS */}
{finansLinks.length > 0 && (
  <>
    {collapsed ? <CollapsedDivider /> : <GroupHeader label="Finans" groupKey="finans" show />}
    {(collapsed || openGroups.finans) &&
      finansLinks.map(link => <NavLink key={link.href} link={link} showLabel={!collapsed} />)
    }
  </>
)}
```

`const finansLinks = filterByAllowed(FINANS_LINKS);` satırını, `gorevlerLinks`
tanımının hemen altına ekle (bkz. dosyada `const gorevlerLinks = filterByAllowed(...)`
satırı).

`openGroups` state'inin başlangıç objesine `finans: false,` ekle (dosyanın en
başındaki `useState<Record<string, boolean>>({ ... })` bloğu).

`allGroups` map'ine (pathname-eşleştirme effect'i içinde) `finans: FINANS_LINKS,`
ekle.

- [ ] **Step 3: Permission eşleştirmesini ekle**

`NAV_PERMISSION_BY_HREF` objesine ekle:

```ts
  "/finans/gelir-gider": "finans_gelir_gider:read",
  "/finans/masraf-talebi": "finans_masraf_talebi:read",
```

- [ ] **Step 4: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```

- [ ] **Step 5: Canlı doğrulama**

Dev sunucusunda herhangi bir sayfaya gir, sidebar'da "Finans" grubunun (personel
rolündeki bir kullanıcı için sadece "Masraf Talebi" linkiyle, yönetici/admin için
hem "Gelir-Gider" hem "Masraf Talebi" ile) göründüğünü doğrula. Linklere tıklayınca
ilgili sayfalar açılmalı.

- [ ] **Step 6: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add components/Nav.tsx
git commit -m "Finans Faz 1: sidebar'a Finans grubu ekle"
```

---

### Task 12: Uçtan uca smoke test ve temizlik

**Files:** yok (sadece doğrulama)

- [ ] **Step 1: Tüm Faz 1 sayfalarını sırayla ziyaret et, konsol/network hatasız
  yüklendiğini doğrula**

`/admin/finans/hesap-plani`, `/admin/finans/kategori`, `/admin/finans/masraf-merkezi`,
`/admin/finans/proje`, `/admin/finans/kasa-banka`, `/admin/finans/vergi-kodu`,
`/admin/finans/odeme-yontemi`, `/finans/gelir-gider`, `/finans/masraf-talebi`.

- [ ] **Step 2: `tsc --noEmit` son kez çalıştır**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```
Beklenen: sıfır hata.

- [ ] **Step 3: Test verilerini temizle (opsiyonel — kullanıcıya sor)**

Task 4-10'da eklenen örnek kayıtlar (770 hesabı, Yakıt Gideri kategorisi, Genel
Merkez masraf merkezi, Ofis Yenileme projesi, Ana Kasa/İş Bankası hesapları, KDV
%20, Nakit/Havale ödeme yöntemleri, örnek gider kaydı, "Klavye alımı" masraf talebi)
gerçek tanım verisi olarak kalabilir ya da kullanıcı isterse silinir — bu, kod
tarafında bir "silme scripti" gerektirmez, ilgili sayfalardan manuel silinir.

- [ ] **Step 4: Faz 1'i tamamlandı olarak işaretle, Faz 2'ye geçiş için kullanıcıya
  haber ver**

Faz 2 (Fatura + Fiş/Belge + Kasa-Banka-Ödeme) için ayrı bir plan dosyası
(`docs/superpowers/plans/YYYY-MM-DD-finans-faz2.md`) bu plan tamamlandıktan sonra
yazılacak — spec'teki sıralama gerekçesi bunu gerektiriyor (Faz 2, Faz 1'deki
kategori/hesap/cari/kasa-banka tanımlarına bağımlı).
