# Finans Modülü — Faz 2 Implementasyon Planı

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Faz 1'in gelir-gider çekirdeğinin üzerine iç fatura (satış/alış),
fiş (gider/tahsilat/tediye/kasa/banka/virman/mahsup/açılış-kapanış/personel
masraf), belge yönetimi (yükleme + mükerrerlik kontrolü) ve ödeme/tahsilat
(fatura ile N:N eşleştirme, banka hareketi manuel eşleştirme) katmanlarını
kurmak.

**Architecture:** Faz 1'deki aynı desenin devamı: migration + `hasPermission`
korumalı REST-benzeri API route'ları + `"use client"` liste+form sayfaları.
Yeni olan tek altyapı parçası: dosya yükleme (`lib/uploads-finans.ts`) —
mevcut `lib/uploads.ts`'teki denetim-fotoğrafı deseninin genelleştirilmiş
hâli (PDF/görsel, SHA-256 hash ile mükerrerlik kontrolü).

**Tech Stack:** Faz 1 ile aynı — Next.js 16 App Router, TypeScript, MySQL/
MariaDB (mysql2/promise), Zod, Tailwind.

## Global Constraints

- Faz 1'in tüm Global Constraints'i geçerli: VARCHAR(36) id, VARCHAR(30)
  ISO timestamp, gerçek tarihler DATE, `ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
  COLLATE=utf8mb4_unicode_ci`, birim test çalıştırıcısı yok (doğrulama =
  `node node_modules/typescript/bin/tsc --noEmit` + canlı DB/tarayıcı
  kontrolü), tünel/dev-server kontrol komutları aynı (bkz. Faz 1 planının
  Global Constraints'i, buraya tekrar yapıştırılmıyor — implementer bu
  komutları Faz 1 task'larından zaten biliyor, brief'lerde de tekrarlanacak).
- Migration dosyası **077** (bir sonraki boş numara).
- **Route çakışması uyarısı:** `/belgeler` zaten var (araç/sürücü evrak
  vade takibi, `documents` tablosu, alakasız modül). Bu fazın belge yönetimi
  sayfası `/finans/belgeler` olacak, ASLA `/belgeler`'e dokunulmayacak.
- Yüklenen dosyalar `data/uploads/finans-belgeler/` altına yazılır — bu yol
  zaten mevcut `denetim_photos` Docker volume'ünün kapsadığı `data/uploads`
  ebeveyn dizininin altında olduğu için **docker-compose.yml'de yeni volume
  gerekmiyor** (bkz. mevcut `denetim_photos:/app/data/uploads` mount'u).
- E-Fatura/E-Arşiv gönderimi, GİB entegrasyonu, OCR motoru: kapsam dışı
  (master spec'te zaten kesinleşti). Belge tablosundaki `ocr_*` alanları
  manuel girilir veya boş kalır.
- Banka hareketi importu CSV/manuel — banka API'si yok.

---

## Dosya Yapısı Özeti

```
migrations/077_finans_fatura_fis_belge.sql   (yeni — Task 1)
lib/permissions.ts                            (değişiklik — Task 2)
lib/schemas.ts                                (değişiklik — Task 3)
lib/uploads-finans.ts                         (yeni — Task 4)

app/api/finans/belge-turu/route.ts            (yeni — Task 5)
app/api/finans/belge-turu/[id]/route.ts       (yeni — Task 5)
app/admin/finans/belge-turu/page.tsx          (yeni — Task 5)

app/api/finans/fatura/route.ts                (yeni — Task 6)
app/api/finans/fatura/[id]/route.ts           (yeni — Task 6)
app/finans/faturalar/page.tsx                 (yeni — Task 6)

app/api/finans/fis/route.ts                   (yeni — Task 7)
app/api/finans/fis/[id]/route.ts              (yeni — Task 7)
app/finans/fisler/page.tsx                    (yeni — Task 7)

app/api/finans/belge/route.ts                 (yeni — Task 8)
app/api/finans/belge/[id]/route.ts            (yeni — Task 8)
app/api/uploads/finans-belge/[filename]/route.ts (yeni — Task 8)
app/finans/belgeler/page.tsx                  (yeni — Task 8)

app/api/finans/odeme/route.ts                 (yeni — Task 9)
app/api/finans/odeme/[id]/route.ts            (yeni — Task 9)
app/finans/odemeler/page.tsx                  (yeni — Task 9)

app/api/finans/banka-hareketi/route.ts        (yeni — Task 10)
app/api/finans/banka-hareketi/[id]/route.ts   (yeni — Task 10)
app/finans/banka-hareketleri/page.tsx         (yeni — Task 10)

components/Nav.tsx                            (değişiklik — Task 11)
```

---

### Task 1: Migration — Faz 2 tabloları

**Files:**
- Create: `migrations/077_finans_fatura_fis_belge.sql`

**Interfaces:**
- Produces: aşağıdaki 8 tablo — sonraki tüm task'lar bu şemayı kullanır.

- [ ] **Step 1: Migration dosyasını yaz**

```sql
-- Migration: 077_finans_fatura_fis_belge
-- Finans modülü Faz 2: belge türü (Faz 1'den taşındı), fatura + kalemi, fiş,
-- belge, ödeme + fatura eşleştirme, banka hareketi.

CREATE TABLE IF NOT EXISTS finans_belge_turu (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  ad VARCHAR(100) NOT NULL,
  numara_serisi_prefix VARCHAR(20),
  sonraki_numara INT NOT NULL DEFAULT 1,
  is_active TINYINT(1) NOT NULL DEFAULT 1,
  created_by VARCHAR(36),
  created_at VARCHAR(30) NOT NULL,
  updated_at VARCHAR(30) NOT NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS finans_fatura (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  tur ENUM('satis','alis') NOT NULL,
  durum ENUM('taslak','onay_bekliyor','onaylandi','muhasebelesti','iptal') NOT NULL DEFAULT 'taslak',
  fatura_no VARCHAR(50),
  belge_turu_id VARCHAR(36) NULL,
  cari_tip ENUM('musteri','tedarikci') NOT NULL,
  cari_id VARCHAR(36) NOT NULL,
  tarih DATE NOT NULL,
  vade_tarihi DATE NULL,
  para_birimi_kod VARCHAR(10) NOT NULL DEFAULT 'TRY',
  kur DECIMAL(12,6) NOT NULL DEFAULT 1,
  ara_toplam DECIMAL(14,2) NOT NULL DEFAULT 0,
  vergi_toplam DECIMAL(14,2) NOT NULL DEFAULT 0,
  genel_toplam DECIMAL(14,2) NOT NULL DEFAULT 0,
  odeme_durumu ENUM('odenmedi','kismen_odendi','odendi','fazla_odendi') NOT NULL DEFAULT 'odenmedi',
  iliskili_fatura_id VARCHAR(36) NULL,
  tekrarlama_json TEXT,
  aciklama TEXT,
  created_by VARCHAR(36) NOT NULL,
  created_at VARCHAR(30) NOT NULL,
  updated_at VARCHAR(30) NOT NULL,
  INDEX idx_fatura_tur_durum (tur, durum),
  INDEX idx_fatura_cari (cari_id),
  INDEX idx_fatura_tarih (tarih),
  FOREIGN KEY (belge_turu_id) REFERENCES finans_belge_turu(id) ON DELETE SET NULL,
  FOREIGN KEY (iliskili_fatura_id) REFERENCES finans_fatura(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS finans_fatura_kalemi (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  fatura_id VARCHAR(36) NOT NULL,
  urun_hizmet_adi VARCHAR(300) NOT NULL,
  miktar DECIMAL(10,2) NOT NULL DEFAULT 1,
  birim_fiyat DECIMAL(14,2) NOT NULL DEFAULT 0,
  vergi_kodu_id VARCHAR(36) NULL,
  tutar DECIMAL(14,2) NOT NULL DEFAULT 0,
  masraf_merkezi_id VARCHAR(36) NULL,
  proje_id VARCHAR(36) NULL,
  department_id VARCHAR(36) NULL,
  INDEX idx_kalem_fatura (fatura_id),
  FOREIGN KEY (fatura_id) REFERENCES finans_fatura(id) ON DELETE CASCADE,
  FOREIGN KEY (vergi_kodu_id) REFERENCES finans_vergi_kodu(id) ON DELETE SET NULL,
  FOREIGN KEY (masraf_merkezi_id) REFERENCES finans_masraf_merkezi(id) ON DELETE SET NULL,
  FOREIGN KEY (proje_id) REFERENCES finans_proje(id) ON DELETE SET NULL,
  FOREIGN KEY (department_id) REFERENCES departments(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS finans_belge (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  dosya_adi VARCHAR(255) NOT NULL,
  dosya_yolu VARCHAR(500) NOT NULL,
  mime_type VARCHAR(100) NOT NULL,
  boyut_bayt INT NOT NULL,
  dosya_hash CHAR(64) NOT NULL,
  ocr_tarih DATE NULL,
  ocr_tutar DECIMAL(14,2) NULL,
  ocr_firma VARCHAR(300) NULL,
  ocr_vergi_no VARCHAR(50) NULL,
  ocr_belge_no VARCHAR(100) NULL,
  yorum_json TEXT,
  versiyon INT NOT NULL DEFAULT 1,
  iliskili_tip ENUM('fatura','fis','gelir_gider','masraf_talebi') NULL,
  iliskili_id VARCHAR(36) NULL,
  created_by VARCHAR(36) NOT NULL,
  created_at VARCHAR(30) NOT NULL,
  INDEX idx_belge_hash (dosya_hash),
  INDEX idx_belge_iliskili (iliskili_tip, iliskili_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS finans_fis (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  tip ENUM('gider_fisi','tahsilat_makbuzu','tediye_makbuzu','kasa_giris','kasa_cikis',
           'banka_islem','virman','mahsup','acilis_kapanis','personel_masraf') NOT NULL,
  tarih DATE NOT NULL,
  tutar DECIMAL(14,2) NOT NULL,
  kasa_banka_hesabi_id VARCHAR(36) NULL,
  karsi_hesap_id VARCHAR(36) NULL,
  belge_id VARCHAR(36) NULL,
  aciklama TEXT,
  created_by VARCHAR(36) NOT NULL,
  created_at VARCHAR(30) NOT NULL,
  updated_at VARCHAR(30) NOT NULL,
  INDEX idx_fis_tip_tarih (tip, tarih),
  FOREIGN KEY (kasa_banka_hesabi_id) REFERENCES finans_kasa_banka_hesabi(id) ON DELETE SET NULL,
  FOREIGN KEY (karsi_hesap_id) REFERENCES finans_kasa_banka_hesabi(id) ON DELETE SET NULL,
  FOREIGN KEY (belge_id) REFERENCES finans_belge(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS finans_odeme (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  tutar DECIMAL(14,2) NOT NULL,
  tarih DATE NOT NULL,
  kasa_banka_hesabi_id VARCHAR(36) NOT NULL,
  odeme_yontemi_id VARCHAR(36) NULL,
  cari_tip ENUM('musteri','tedarikci') NOT NULL,
  cari_id VARCHAR(36) NOT NULL,
  aciklama TEXT,
  created_by VARCHAR(36) NOT NULL,
  created_at VARCHAR(30) NOT NULL,
  INDEX idx_odeme_cari (cari_id),
  INDEX idx_odeme_tarih (tarih),
  FOREIGN KEY (kasa_banka_hesabi_id) REFERENCES finans_kasa_banka_hesabi(id),
  FOREIGN KEY (odeme_yontemi_id) REFERENCES finans_odeme_yontemi(id) ON DELETE SET NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS finans_odeme_fatura (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  odeme_id VARCHAR(36) NOT NULL,
  fatura_id VARCHAR(36) NOT NULL,
  tutar DECIMAL(14,2) NOT NULL,
  created_at VARCHAR(30) NOT NULL,
  INDEX idx_of_odeme (odeme_id),
  INDEX idx_of_fatura (fatura_id),
  FOREIGN KEY (odeme_id) REFERENCES finans_odeme(id) ON DELETE CASCADE,
  FOREIGN KEY (fatura_id) REFERENCES finans_fatura(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE IF NOT EXISTS finans_banka_hareketi (
  id VARCHAR(36) NOT NULL PRIMARY KEY,
  kasa_banka_hesabi_id VARCHAR(36) NOT NULL,
  tarih DATE NOT NULL,
  aciklama VARCHAR(500),
  tutar DECIMAL(14,2) NOT NULL,
  yon ENUM('gelen','giden') NOT NULL,
  eslesen_tip ENUM('fatura','odeme') NULL,
  eslesen_id VARCHAR(36) NULL,
  created_by VARCHAR(36) NOT NULL,
  created_at VARCHAR(30) NOT NULL,
  INDEX idx_bh_hesap_tarih (kasa_banka_hesabi_id, tarih),
  INDEX idx_bh_eslesen (eslesen_tip, eslesen_id),
  FOREIGN KEY (kasa_banka_hesabi_id) REFERENCES finans_kasa_banka_hesabi(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

- [ ] **Step 2: Tünel ve dev sunucusunu ayağa kaldır**

```bash
nc -z -w3 127.0.0.1 3307 || (pkill -f "ssh.*3307"; sshpass -p 'kayra123' ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=15 -o ServerAliveCountMax=3 -f -N -L 3307:172.22.0.3:3306 root@212.64.201.150)
pkill -f "next dev"; sleep 1
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && nohup npm run dev > /tmp/aycanops-dev.log 2>&1 &
sleep 8
```

- [ ] **Step 3: Migration'ın uygulandığını doğrula**

```bash
grep "077_finans_fatura_fis_belge" /tmp/aycanops-dev.log
```
Beklenen: `Migration 077_finans_fatura_fis_belge.sql applied successfully`.

- [ ] **Step 4: Tabloların oluştuğunu DB'den doğrula**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node -e "
const mysql = require('mysql2/promise');
(async () => {
  const conn = await mysql.createConnection({ host: '127.0.0.1', port: 3307, user: 'aycanops', password: 'Kayra2190.', database: 'aycanops_db' });
  const [rows] = await conn.query(\"SHOW TABLES LIKE 'finans_%'\");
  console.log(rows);
  await conn.end();
})().catch(e => { console.error(e.message); process.exit(1); });
"
```
Beklenen: Faz 1'in 10 tablosu + bu fazın 8 tablosu = 18 `finans_%` tablosu.

- [ ] **Step 5: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add migrations/077_finans_fatura_fis_belge.sql
git commit -m "Finans Faz 2: fatura+kalemi, fiş, belge, ödeme, banka hareketi tabloları"
```

---

### Task 2: Yetkilendirme — `lib/permissions.ts`

**Files:**
- Modify: `lib/permissions.ts`

**Interfaces:**
- Consumes: mevcut `PERMISSIONS` kataloğu ve `DEFAULT_ROLE_PERMISSIONS` yapısı
  (Faz 1'de zaten değiştirilen dosya — aynı desende devam).
- Produces: `"finans_fatura:read"` gibi yeni `PermissionKey` string'leri.

- [ ] **Step 1: `PERMISSIONS` kataloğuna ekle**

```ts
  finans_belge_turu: ["read", "create", "update", "delete"],
  finans_fatura: ["read", "create", "update", "delete", "approve"],
  finans_fis: ["read", "create", "update", "delete"],
  finans_belge: ["read", "create", "delete"],
  finans_odeme: ["read", "create", "delete"],
  finans_banka_hareketi: ["read", "create", "update"],
```

- [ ] **Step 2: `DEFAULT_ROLE_PERMISSIONS.yetkili`'ye okuma ekle**

```ts
    "finans_fatura:read",
    "finans_fis:read",
    "finans_belge:read",
    "finans_odeme:read",
    "finans_banka_hareketi:read",
    "finans_belge_turu:read",
```

- [ ] **Step 3: `DEFAULT_ROLE_PERMISSIONS.yonetici`'ye tam yönetim ekle**

```ts
    "finans_belge_turu:read",
    "finans_belge_turu:create",
    "finans_belge_turu:update",
    "finans_fatura:read",
    "finans_fatura:create",
    "finans_fatura:update",
    "finans_fatura:approve",
    "finans_fis:read",
    "finans_fis:create",
    "finans_fis:update",
    "finans_belge:read",
    "finans_belge:create",
    "finans_belge:delete",
    "finans_odeme:read",
    "finans_odeme:create",
    "finans_banka_hareketi:read",
    "finans_banka_hareketi:create",
    "finans_banka_hareketi:update",
```

(`admin` otomatik — kod değişikliği gerekmiyor, Faz 1'deki aynı flatMap satırı.)

- [ ] **Step 4: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```

- [ ] **Step 5: Mevcut sistem rollerine DB'de de yükle**

Faz 1 Task 2'deki yöntemle aynı: `/admin/roller`'dan Yönetici rolüne yukarıdaki
tüm yeni izinleri ekle (veya sayfa içinden `GET`+`PUT
/api/admin/roles/<yonetici-id>/permissions` ile toplu ekle), Yetkili'ye de
okuma izinlerini ekle.

- [ ] **Step 6: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add lib/permissions.ts
git commit -m "Finans Faz 2: fatura/fiş/belge/ödeme/banka-hareketi izinleri"
```

---

### Task 3: Zod şemaları — `lib/schemas.ts`

**Files:**
- Modify: `lib/schemas.ts`

- [ ] **Step 1: Şemaları ekle**

```ts
// ─── Finans: Belge Türü ─────────────────────────────────────────────────
export const finansBelgeTuruSchema = z.object({
  ad: shortStr(100),
  numara_serisi_prefix: z.string().max(20).optional().nullable(),
});

// ─── Finans: Fatura ─────────────────────────────────────────────────────
export const finansFaturaKalemiSchema = z.object({
  urun_hizmet_adi: shortStr(300),
  miktar: z.number().min(0.01),
  birim_fiyat: z.number().min(0),
  vergi_kodu_id: z.string().optional().nullable(),
  masraf_merkezi_id: z.string().optional().nullable(),
  proje_id: z.string().optional().nullable(),
  department_id: z.string().optional().nullable(),
});

export const finansFaturaSchema = z.object({
  tur: z.enum(["satis", "alis"]),
  belge_turu_id: z.string().optional().nullable(),
  cari_tip: z.enum(["musteri", "tedarikci"]),
  cari_id: shortStr(36),
  tarih: z.string().min(1),
  vade_tarihi: z.string().optional().nullable(),
  para_birimi_kod: z.string().max(10).optional(),
  kur: z.number().optional(),
  iliskili_fatura_id: z.string().optional().nullable(),
  aciklama: z.string().max(2000).optional().nullable(),
  kalemler: z.array(finansFaturaKalemiSchema).min(1, "En az bir kalem gerekli"),
});

// ─── Finans: Fiş ─────────────────────────────────────────────────────────
export const finansFisSchema = z.object({
  tip: z.enum([
    "gider_fisi", "tahsilat_makbuzu", "tediye_makbuzu", "kasa_giris", "kasa_cikis",
    "banka_islem", "virman", "mahsup", "acilis_kapanis", "personel_masraf",
  ]),
  tarih: z.string().min(1),
  tutar: z.number(),
  kasa_banka_hesabi_id: z.string().optional().nullable(),
  karsi_hesap_id: z.string().optional().nullable(),
  belge_id: z.string().optional().nullable(),
  aciklama: z.string().max(2000).optional().nullable(),
});

// ─── Finans: Ödeme ──────────────────────────────────────────────────────
export const finansOdemeSchema = z.object({
  tutar: z.number().min(0.01),
  tarih: z.string().min(1),
  kasa_banka_hesabi_id: shortStr(36),
  odeme_yontemi_id: z.string().optional().nullable(),
  cari_tip: z.enum(["musteri", "tedarikci"]),
  cari_id: shortStr(36),
  aciklama: z.string().max(2000).optional().nullable(),
  fatura_eslesme: z.array(z.object({
    fatura_id: z.string(),
    tutar: z.number().min(0.01),
  })).optional(),
});

// ─── Finans: Banka Hareketi ─────────────────────────────────────────────
export const finansBankaHareketiSchema = z.object({
  kasa_banka_hesabi_id: shortStr(36),
  tarih: z.string().min(1),
  aciklama: z.string().max(500).optional().nullable(),
  tutar: z.number(),
  yon: z.enum(["gelen", "giden"]),
});
```

- [ ] **Step 2: Tip kontrolü ve commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
git add lib/schemas.ts
git commit -m "Finans Faz 2: fatura/fiş/ödeme/banka-hareketi Zod şemaları"
```

---

### Task 4: Genel belge yükleme yardımcı modülü

**Files:**
- Create: `lib/uploads-finans.ts`

**Interfaces:**
- Consumes: mevcut `lib/uploads.ts`'teki desen (referans, kopyalanmaz — bu
  yeni dosya bağımsız, çünkü mime seti ve hedef dizin farklı).
- Produces: `saveFinansBelge`, `readFinansBelgePath`, `deleteFinansBelge`,
  `isSafeFinansFilename`, `computeFileHash`, `isAllowedFinansMime`,
  `extForFinansMime`, `MAX_BELGE_BYTES` — Task 8 bunları kullanır.

- [ ] **Step 1: Referans dosyayı oku**

`/Users/kayraisbilir/Documents/tasinios/aycanops/aycanops/lib/uploads.ts`'i
oku — aynı güvenlik desenini (ayrı, deploy'dan etkilenmeyen dizin; path
traversal koruması için sıkı dosya adı regex'i; mkdir/writeFile/unlink) takip
et, ama şu farklarla:

- [ ] **Step 2: Dosyayı yaz**

```ts
import { mkdir, writeFile, unlink } from "fs/promises";
import { createHash } from "crypto";
import path from "path";

// public/ dizini deploy sırasında ezilebiliyor — yüklenen dosyalar ayrı,
// kalıcı bir yola yazılır. Bu yol mevcut denetim_photos Docker volume'ünün
// (data/uploads) kapsadığı ebeveyn dizinin altında olduğu için yeni bir
// volume tanımlamaya gerek yok.
const UPLOAD_ROOT = process.env.FINANS_BELGE_UPLOAD_DIR || path.join(process.cwd(), "data", "uploads", "finans-belgeler");

const ALLOWED_MIME: Record<string, string> = {
  "application/pdf": "pdf",
  "image/jpeg": "jpg",
  "image/png": "png",
  "image/webp": "webp",
  "application/xml": "xml",
  "text/xml": "xml",
};

export const MAX_BELGE_BYTES = 15 * 1024 * 1024; // 15MB

export function isAllowedFinansMime(mimeType: string): boolean {
  return mimeType in ALLOWED_MIME;
}

export function extForFinansMime(mimeType: string): string {
  return ALLOWED_MIME[mimeType] || "bin";
}

export function computeFileHash(buffer: Buffer): string {
  return createHash("sha256").update(buffer).digest("hex");
}

export async function saveFinansBelge(filename: string, buffer: Buffer): Promise<void> {
  await mkdir(UPLOAD_ROOT, { recursive: true });
  await writeFile(path.join(UPLOAD_ROOT, filename), buffer);
}

export async function readFinansBelgePath(filename: string): Promise<string> {
  return path.join(UPLOAD_ROOT, filename);
}

export async function deleteFinansBelge(filename: string): Promise<void> {
  try {
    await unlink(path.join(UPLOAD_ROOT, filename));
  } catch {
    // dosya zaten yoksa sessizce geç
  }
}

// Path traversal koruması: filename sadece uuid.ext formatında olmalı
export function isSafeFinansFilename(filename: string): boolean {
  return /^[a-f0-9-]+\.(pdf|jpg|jpeg|png|webp|xml)$/i.test(filename);
}
```

- [ ] **Step 3: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add lib/uploads-finans.ts
git commit -m "Finans Faz 2: genel belge yükleme yardımcı modülü"
```

---

### Task 5: Belge Türü (API + sayfa)

**Files:**
- Create: `app/api/finans/belge-turu/route.ts`
- Create: `app/api/finans/belge-turu/[id]/route.ts`
- Create: `app/admin/finans/belge-turu/page.tsx`

**Interfaces:**
- Consumes: `finansBelgeTuruSchema` (Task 3).
- Produces: `GET /api/finans/belge-turu` → Task 6'daki fatura formunun
  "belge türü" seçiminde kullanılabilir (opsiyonel alan).

- [ ] **Step 1-3: Faz 1 Task 4'ün (Hesap Planı) birebir aynı deseni** —
  `app/api/finans/hesap-plani/route.ts` ve `[id]/route.ts`'i temel al, tablo
  `finans_belge_turu`, şema `finansBelgeTuruSchema`, kaynak `finans_belge_turu`,
  alanlar `ad`, `numara_serisi_prefix` (opsiyonel). Sayfa formu: `ad`,
  `numara_serisi_prefix` (örn. "FTR-", "GDF-"). Başlık: "Belge Türleri /
  Numara Serileri".

- [ ] **Step 4: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```

- [ ] **Step 5: Canlı doğrulama**

`/admin/finans/belge-turu`'nda "Satış Faturası" (prefix "FTR-") ve "Gider
Fişi" (prefix "GDF-") oluştur, listede görünmeli.

- [ ] **Step 6: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add app/api/finans/belge-turu app/admin/finans/belge-turu
git commit -m "Finans Faz 2: Belge Türü API + sayfa"
```

---

### Task 6: Fatura ana modülü (satış/alış + kalemler)

**Files:**
- Create: `app/api/finans/fatura/route.ts`
- Create: `app/api/finans/fatura/[id]/route.ts`
- Create: `app/finans/faturalar/page.tsx`

**Interfaces:**
- Consumes: `finansFaturaSchema` (kalemler dahil, Task 3), `GET
  /api/finans/vergi-kodu` (Faz 1, kalem vergi kodu seçimi), `GET
  /api/finans/masraf-merkezi`/`proje` (Faz 1), `GET /api/departments`
  (mevcut), `GET /api/companies`/`GET /api/isletenler` (mevcut, cari seçimi —
  `cari_tip=musteri` ise companies, `cari_tip=tedarikci` ise isletenler).
- Produces: `GET /api/finans/fatura` → `{ ok, data: [{...fatura, kalemler:
  [...]}] }` — her fatura kendi kalemleriyle birlikte döner.

- [ ] **Step 1: List+create route'unu yaz**

`app/api/finans/fatura/route.ts` — bu görevin en karmaşık API'si, transaction
benzeri çok-satırlı INSERT gerektiriyor (fatura başlığı + N kalem). Bu
depoda SQL transaction kullanan bir örnek yoksa (kontrol et:
`grep -rn "db.transaction" app/api/`), sıralı (transaction'sız) INSERT'ler
yeterli — depo genelinde bu yaklaşım kullanılıyor, tutarlı ol:

```ts
import { NextRequest, NextResponse } from "next/server";
import { getDb } from "@/lib/db";
import { requireUser } from "@/lib/auth";
import { hasPermission } from "@/lib/permissions";
import { v4 as uuidv4 } from "uuid";
import { nowIso } from "@/lib/time";
import { apiError } from "@/lib/api-error";
import { finansFaturaSchema } from "@/lib/schemas";

export async function GET(req: NextRequest) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });
    if (!hasPermission(user, "finans_fatura:read"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });

    const { searchParams } = new URL(req.url);
    const tur = searchParams.get("tur");
    const durum = searchParams.get("durum");
    const conditions: string[] = [];
    const params: unknown[] = [];
    if (tur) { conditions.push("f.tur = ?"); params.push(tur); }
    if (durum) { conditions.push("f.durum = ?"); params.push(durum); }
    const where = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";

    const db = getDb();
    const faturalar = await db.prepare(
      `SELECT f.*, bt.ad AS belge_turu_ad
       FROM finans_fatura f
       LEFT JOIN finans_belge_turu bt ON bt.id = f.belge_turu_id
       ${where}
       ORDER BY f.tarih DESC, f.created_at DESC`
    ).all(...params) as any[];

    if (faturalar.length > 0) {
      const ids = faturalar.map(f => f.id);
      const kalemler = await db.prepare(
        `SELECT * FROM finans_fatura_kalemi WHERE fatura_id IN (${ids.map(() => "?").join(",")})`
      ).all(...ids) as any[];
      const kalemlerByFatura: Record<string, any[]> = {};
      for (const k of kalemler) {
        if (!kalemlerByFatura[k.fatura_id]) kalemlerByFatura[k.fatura_id] = [];
        kalemlerByFatura[k.fatura_id].push(k);
      }
      for (const f of faturalar) f.kalemler = kalemlerByFatura[f.id] || [];
    }

    return NextResponse.json({ ok: true, data: faturalar });
  } catch (e) { return apiError(e); }
}

export async function POST(req: NextRequest) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });
    if (!hasPermission(user, "finans_fatura:create"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });

    const raw = await req.json();
    const parsed = finansFaturaSchema.safeParse(raw);
    if (!parsed.success) return NextResponse.json({ ok: false, error: parsed.error.flatten().fieldErrors }, { status: 400 });
    const d = parsed.data;

    // Toplamlar sunucuda hesaplanır — istemciden gelen değerler kullanılmaz
    // (Faz 1'in gelir-gider'inde uygulanan aynı ilke: brüt/toplam alanları
    // her zaman kalem verisinden türetilir).
    let araToplam = 0;
    let vergiToplam = 0;
    for (const k of d.kalemler) araToplam += k.miktar * k.birim_fiyat;
    // Basit yaklaşım: vergi kodu oranı ayrıca sorgulanabilir; Faz 2 MVP'de
    // kalem bazlı vergi toplamı, ilgili vergi_kodu_id'nin `finans_vergi_kodu.oran`
    // değeri üzerinden hesaplanır.
    const vergiKoduIds = [...new Set(d.kalemler.map(k => k.vergi_kodu_id).filter(Boolean))] as string[];
    const db = getDb();
    const oranMap: Record<string, number> = {};
    if (vergiKoduIds.length > 0) {
      const oranlar = await db.prepare(
        `SELECT id, oran FROM finans_vergi_kodu WHERE id IN (${vergiKoduIds.map(() => "?").join(",")})`
      ).all(...vergiKoduIds) as { id: string; oran: number }[];
      for (const o of oranlar) oranMap[o.id] = Number(o.oran);
    }
    for (const k of d.kalemler) {
      const kalemTutar = k.miktar * k.birim_fiyat;
      const oran = k.vergi_kodu_id ? (oranMap[k.vergi_kodu_id] ?? 0) : 0;
      vergiToplam += kalemTutar * (oran / 100);
    }
    const genelToplam = araToplam + vergiToplam;

    const id = uuidv4();
    const now = nowIso();
    await db.prepare(
      `INSERT INTO finans_fatura
         (id, tur, durum, belge_turu_id, cari_tip, cari_id, tarih, vade_tarihi,
          para_birimi_kod, kur, ara_toplam, vergi_toplam, genel_toplam, odeme_durumu,
          iliskili_fatura_id, aciklama, created_by, created_at, updated_at)
       VALUES (?, ?, 'taslak', ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, 'odenmedi', ?, ?, ?, ?, ?)`
    ).run(
      id, d.tur, d.belge_turu_id || null, d.cari_tip, d.cari_id, d.tarih, d.vade_tarihi || null,
      d.para_birimi_kod || "TRY", d.kur ?? 1, araToplam, vergiToplam, genelToplam,
      d.iliskili_fatura_id || null, d.aciklama || null, user.id, now, now
    );

    for (const k of d.kalemler) {
      const kalemTutar = k.miktar * k.birim_fiyat;
      await db.prepare(
        `INSERT INTO finans_fatura_kalemi
           (id, fatura_id, urun_hizmet_adi, miktar, birim_fiyat, vergi_kodu_id, tutar,
            masraf_merkezi_id, proje_id, department_id)
         VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`
      ).run(
        uuidv4(), id, k.urun_hizmet_adi, k.miktar, k.birim_fiyat, k.vergi_kodu_id || null,
        kalemTutar, k.masraf_merkezi_id || null, k.proje_id || null, k.department_id || null
      );
    }

    return NextResponse.json({ ok: true, data: { id } }, { status: 201 });
  } catch (e) { return apiError(e); }
}
```

- [ ] **Step 2: Update+delete+durum route'unu yaz**

`app/api/finans/fatura/[id]/route.ts` — Faz 1'in gelir-gider `[id]/route.ts`
desenini (durum makinesi: taslak → onay_bekliyor → onaylandı/iptal, onaylanmış
kayıt düzenlenemez/silinemez, `finans_fatura:approve` ile onay) uyarla. Kalem
güncellemesi bu fazda desteklenmiyor (basitleştirme — sadece başlık alanları
ve durum değişir; kalem düzeltmek isteyen kullanıcı faturayı iptal edip
yeniden oluşturur). PUT body'de `action: "onayla"` durumu `onaylandi` yapar
(ekstra onay zinciri yok, tek adımlı — Faz 1'in dört-göz kuralı burada
uygulanmaz çünkü fatura kendi firmasının kaydı, iç onay değil).

- [ ] **Step 3: Sayfayı yaz**

`app/finans/faturalar/page.tsx` — üstte "Satış" / "Alış" sekme toggle'ı,
altında liste (tur filtresine göre otomatik filtrelenir) + slide-over form:
- Form: `cari_tip`+`cari_id` (tur=satis ise companies, tur=alis ise
  isletenler listesinden dropdown), `tarih`/`vade_tarihi` (date input,
  tarih varsayılanı `todayIstanbul()`), `belge_turu_id` (dropdown, opsiyonel),
  `para_birimi_kod` (TRY/USD/EUR), `kur` (Faz 1'deki gelir-gider sayfasının
  aynı deseni — TRY'de disabled/1'e sabit), kalem listesi (dinamik satır
  ekle/çıkar: `urun_hizmet_adi`, `miktar`, `birim_fiyat`, `vergi_kodu_id`
  dropdown — her satırın tutarı miktar×birim_fiyat olarak canlı hesaplanıp
  gösterilir, salt-okunur), altta ara toplam/vergi toplam/genel toplam
  (sunucudan dönen değerle senkron, form açıkken tahmini olarak client-side
  de hesaplanabilir ama otorite her zaman sunucudur).
- Liste satırı: fatura_no (varsa) veya "—", cari adı (companies/isletenler
  listesinden `cari_id` eşleştirilerek), tarih, genel_toplam+para_birimi_kod,
  durum rozeti, ödeme_durumu rozeti.
- Onay butonu: `hasPermission(user, "finans_fatura:approve")` ile gizlenir.

- [ ] **Step 4: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```

- [ ] **Step 5: Canlı doğrulama**

`/finans/faturalar`'da tur=satis, bir firma seçip, tek kalem (ürün="Servis
Ücreti", miktar=1, birim_fiyat=1000, vergi_kodu=KDV %20) ile fatura oluştur.
DB'den doğrula:

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node -e "
const mysql = require('mysql2/promise');
(async () => {
  const conn = await mysql.createConnection({ host: '127.0.0.1', port: 3307, user: 'aycanops', password: 'Kayra2190.', database: 'aycanops_db' });
  const [rows] = await conn.query('SELECT ara_toplam, vergi_toplam, genel_toplam FROM finans_fatura ORDER BY created_at DESC LIMIT 1');
  console.log(rows);
  await conn.end();
})().catch(e => { console.error(e.message); process.exit(1); });
"
```
Beklenen: `ara_toplam: 1000, vergi_toplam: 200, genel_toplam: 1200`.

- [ ] **Step 6: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add app/api/finans/fatura app/finans/faturalar
git commit -m "Finans Faz 2: Fatura ana modülü (satış/alış + kalemler)"
```

---

### Task 7: Fiş modülü

**Files:**
- Create: `app/api/finans/fis/route.ts`
- Create: `app/api/finans/fis/[id]/route.ts`
- Create: `app/finans/fisler/page.tsx`

**Interfaces:**
- Consumes: `finansFisSchema` (Task 3), `GET /api/finans/kasa-banka` (Faz 1).
- Produces: bağımsız modül, başka task tarafından tüketilmiyor.

- [ ] **Step 1-2: Route'ları yaz** (Faz 1 Task 4 deseni, tablo `finans_fis`,
  alanlar `tip`, `tarih`, `tutar`, `kasa_banka_hesabi_id`, `karsi_hesap_id`,
  `belge_id`, `aciklama`; kaynak `finans_fis`. `karsi_hesap_id` sadece
  `tip==="virman"` iken anlamlı ama şema seviyesinde zorunlu değil.)

- [ ] **Step 3: Sayfayı yaz**

`app/finans/fisler/page.tsx` — form: `tip` (10 seçenekli select, Türkçe
etiketler: "Gider Fişi", "Tahsilat Makbuzu", "Tediye Makbuzu", "Kasa Girişi",
"Kasa Çıkışı", "Banka İşlemi", "Virman", "Mahsup", "Açılış/Kapanış",
"Personel Masraf Formu"), `tarih`, `tutar`, `kasa_banka_hesabi_id` (dropdown,
Faz 1'in kasa-banka listesinden), `karsi_hesap_id` (dropdown, sadece
tip==="virman" iken göster), `aciklama`. Liste: tip rozeti, tarih, tutar,
hesap adı. Filtre: tip'e göre.

- [ ] **Step 4: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```

- [ ] **Step 5: Canlı doğrulama**

`/finans/fisler`'da tip="Gider Fişi", tutar=250, kasa_banka=Ana Kasa (Faz 1)
ile bir fiş oluştur, listede görünmeli, DB'de doğrula.

- [ ] **Step 6: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add app/api/finans/fis app/finans/fisler
git commit -m "Finans Faz 2: Fiş modülü"
```

---

### Task 8: Belge yönetimi (yükleme + mükerrerlik kontrolü)

**Files:**
- Create: `app/api/finans/belge/route.ts`
- Create: `app/api/finans/belge/[id]/route.ts`
- Create: `app/api/uploads/finans-belge/[filename]/route.ts`
- Create: `app/finans/belgeler/page.tsx`

**Interfaces:**
- Consumes: `lib/uploads-finans.ts` (Task 4) — `saveFinansBelge`,
  `computeFileHash`, `isAllowedFinansMime`, `extForFinansMime`,
  `isSafeFinansFilename`, `readFinansBelgePath`, `MAX_BELGE_BYTES`.
- Reference implementation: `app/api/inspections/[id]/photos/route.ts` ve
  `app/api/uploads/denetim/[filename]/route.ts` (Faz öncesi, denetim
  fotoğrafı özelliği) — aynı multipart-upload + auth-gated-serve deseni,
  farklı mime seti ve mükerrerlik kontrolü ile.

- [ ] **Step 1: List+upload route'unu yaz**

`app/api/finans/belge/route.ts`:

```ts
import { NextRequest, NextResponse } from "next/server";
import { getDb } from "@/lib/db";
import { requireUser } from "@/lib/auth";
import { hasPermission } from "@/lib/permissions";
import { v4 as uuidv4 } from "uuid";
import { nowIso } from "@/lib/time";
import { apiError } from "@/lib/api-error";
import {
  isAllowedFinansMime, extForFinansMime, saveFinansBelge, computeFileHash, MAX_BELGE_BYTES,
} from "@/lib/uploads-finans";

export async function GET(req: NextRequest) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });
    if (!hasPermission(user, "finans_belge:read"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });

    const { searchParams } = new URL(req.url);
    const iliskiliTip = searchParams.get("iliskili_tip");
    const iliskiliId = searchParams.get("iliskili_id");
    const conditions: string[] = [];
    const params: unknown[] = [];
    if (iliskiliTip) { conditions.push("iliskili_tip = ?"); params.push(iliskiliTip); }
    if (iliskiliId) { conditions.push("iliskili_id = ?"); params.push(iliskiliId); }
    const where = conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";

    const rows = await getDb().prepare(
      `SELECT id, dosya_adi, mime_type, boyut_bayt, ocr_tarih, ocr_tutar, ocr_firma,
              ocr_vergi_no, ocr_belge_no, versiyon, iliskili_tip, iliskili_id, created_at
       FROM finans_belge ${where} ORDER BY created_at DESC`
    ).all(...params);
    return NextResponse.json({ ok: true, data: rows });
  } catch (e) { return apiError(e); }
}

export async function POST(req: NextRequest) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });
    if (!hasPermission(user, "finans_belge:create"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });

    const formData = await req.formData();
    const file = formData.get("dosya");
    if (!(file instanceof File)) return NextResponse.json({ ok: false, error: "Dosya bulunamadı" }, { status: 400 });
    if (!isAllowedFinansMime(file.type))
      return NextResponse.json({ ok: false, error: `Desteklenmeyen dosya türü: ${file.type}` }, { status: 400 });
    if (file.size > MAX_BELGE_BYTES)
      return NextResponse.json({ ok: false, error: "Dosya 15MB'ı geçemez" }, { status: 400 });

    const buffer = Buffer.from(await file.arrayBuffer());
    const hash = computeFileHash(buffer);

    const db = getDb();
    const existing = await db.prepare(`SELECT id, dosya_adi FROM finans_belge WHERE dosya_hash = ?`).get(hash) as
      { id: string; dosya_adi: string } | undefined;
    if (existing) {
      return NextResponse.json(
        { ok: false, error: `Bu dosya zaten yüklenmiş: "${existing.dosya_adi}"`, mukerrer_belge_id: existing.id },
        { status: 409 }
      );
    }

    const iliskiliTip = formData.get("iliskili_tip") as string | null;
    const iliskiliId = formData.get("iliskili_id") as string | null;

    const filename = `${uuidv4()}.${extForFinansMime(file.type)}`;
    await saveFinansBelge(filename, buffer);

    const id = uuidv4();
    const now = nowIso();
    await db.prepare(
      `INSERT INTO finans_belge
         (id, dosya_adi, dosya_yolu, mime_type, boyut_bayt, dosya_hash, versiyon,
          iliskili_tip, iliskili_id, created_by, created_at)
       VALUES (?, ?, ?, ?, ?, ?, 1, ?, ?, ?, ?)`
    ).run(id, file.name, filename, file.type, file.size, hash, iliskiliTip || null, iliskiliId || null, user.id, now);

    return NextResponse.json({ ok: true, data: { id, filename } }, { status: 201 });
  } catch (e) { return apiError(e); }
}
```

- [ ] **Step 2: Delete route'unu yaz**

`app/api/finans/belge/[id]/route.ts` — sadece `DELETE` (belge içeriği
değiştirilemez, sadece silinip yeniden yüklenir): `finans_belge:delete`
kontrolü, DB'den `dosya_yolu` oku, `deleteFinansBelge(dosya_yolu)` çağır,
satırı sil.

- [ ] **Step 3: Serve route'unu yaz**

`app/api/uploads/finans-belge/[filename]/route.ts` — `app/api/uploads/
denetim/[filename]/route.ts`'in aynısı: `requireUser()` + `hasPermission(user,
"finans_belge:read")`, `isSafeFinansFilename(filename)` kontrolü, DB'de
`dosya_yolu = filename` eşleşen bir `finans_belge` satırı var mı kontrol et
(yoksa 404 — rastgele dosya adı denemesini engeller), varsa
`readFinansBelgePath` ile oku ve `mime_type` header'ıyla stream et.

- [ ] **Step 4: Sayfayı yaz**

`app/finans/belgeler/page.tsx` — dosya seçici (input type=file) + yükle
butonu, mükerrerlik hatası geldiğinde (`409` + `mukerrer_belge_id`) kullanıcıya
"Bu dosya zaten yüklenmiş" uyarısı göster (toast.error). Liste: dosya adı,
tip ikonu (PDF/görsel), boyut (KB/MB formatlı), yükleme tarihi, "İndir/Görüntüle"
linki (`/api/uploads/finans-belge/${dosya_yolu}` — API'den `dosya_yolu` alanı
dönmüyorsa GET route'unu güncelle, bu alanı da eklemesi gerekir), "Sil" butonu
(`finans_belge:delete`). OCR alanları bu fazda form'da YOK — sadece DB'de
hazır duruyor, manuel doldurma arayüzü Faz 2'nin kapsamı dışında (spec'in
"OCR motoru yok" kısıtına uygun, alanlar boş kalır).

- [ ] **Step 5: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```

- [ ] **Step 6: Canlı doğrulama — yükleme + mükerrerlik**

1. `/Users/kayraisbilir/claude` altında küçük bir test PDF/JPEG oluştur
   (Playwright dosya yükleme sandbox'ı sadece bu dizin altındaki dosyalara
   izin veriyor — `/private/tmp/.../scratchpad` altındaki dosyalar reddedilir,
   önce oraya kopyala).
2. `/finans/belgeler`'de yükle, listede görünmeli.
3. AYNI dosyayı tekrar yüklemeyi dene → 409 + "zaten yüklenmiş" mesajı
   görünmeli (UI'da toast, ayrıca API'yi doğrudan çağırıp 409 döndüğünü
   doğrula).
4. DB'den doğrula: `finans_belge` tablosunda tek satır olmalı (mükerrer
   INSERT olmamalı).

- [ ] **Step 7: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add app/api/finans/belge app/api/uploads/finans-belge app/finans/belgeler
git commit -m "Finans Faz 2: Belge yönetimi (yükleme + mükerrerlik kontrolü)"
```

---

### Task 9: Ödeme modülü + fatura eşleştirme

**Files:**
- Create: `app/api/finans/odeme/route.ts`
- Create: `app/api/finans/odeme/[id]/route.ts`
- Create: `app/finans/odemeler/page.tsx`

**Interfaces:**
- Consumes: `finansOdemeSchema` (Task 3, `fatura_eslesme` alanı dahil),
  `GET /api/finans/fatura` (Task 6) — açık faturaları listelemek için,
  `GET /api/finans/kasa-banka` + `GET /api/finans/odeme-yontemi` (Faz 1).
- Produces: bir ödeme oluşturulunca `finans_odeme_fatura` satırları yazılır
  ve eşleşen faturaların `odeme_durumu` güncellenir — Faz 3'teki cari
  ekstre/yaşlandırma raporu bu veriye dayanacak.

- [ ] **Step 1: List+create route'unu yaz**

`app/api/finans/odeme/route.ts` — POST, `finansOdemeSchema.fatura_eslesme`
dizisindeki her `{fatura_id, tutar}` çifti için `finans_odeme_fatura`'ya bir
satır ekler, sonra ilgili faturanın toplam eşleşen tutarını (bu ödeme +
önceki tüm eşleşmeler) `genel_toplam` ile karşılaştırıp `odeme_durumu`'nu
günceller (`odenmedi` → hiç eşleşme yoksa, `kismen_odendi` → toplam <
genel_toplam, `odendi` → toplam == genel_toplam, `fazla_odendi` → toplam >
genel_toplam):

```ts
import { NextRequest, NextResponse } from "next/server";
import { getDb } from "@/lib/db";
import { requireUser } from "@/lib/auth";
import { hasPermission } from "@/lib/permissions";
import { v4 as uuidv4 } from "uuid";
import { nowIso } from "@/lib/time";
import { apiError } from "@/lib/api-error";
import { finansOdemeSchema } from "@/lib/schemas";

export async function GET(req: NextRequest) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });
    if (!hasPermission(user, "finans_odeme:read"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });

    const rows = await getDb().prepare(
      `SELECT o.*, kb.ad AS kasa_banka_ad, oy.ad AS odeme_yontemi_ad
       FROM finans_odeme o
       LEFT JOIN finans_kasa_banka_hesabi kb ON kb.id = o.kasa_banka_hesabi_id
       LEFT JOIN finans_odeme_yontemi oy ON oy.id = o.odeme_yontemi_id
       ORDER BY o.tarih DESC, o.created_at DESC`
    ).all();
    return NextResponse.json({ ok: true, data: rows });
  } catch (e) { return apiError(e); }
}

export async function POST(req: NextRequest) {
  try {
    const user = await requireUser();
    if (!user) return NextResponse.json({ ok: false, error: "Yetkisiz" }, { status: 401 });
    if (!hasPermission(user, "finans_odeme:create"))
      return NextResponse.json({ ok: false, error: "Yetersiz yetki" }, { status: 403 });

    const raw = await req.json();
    const parsed = finansOdemeSchema.safeParse(raw);
    if (!parsed.success) return NextResponse.json({ ok: false, error: parsed.error.flatten().fieldErrors }, { status: 400 });
    const d = parsed.data;

    const db = getDb();
    const id = uuidv4();
    const now = nowIso();
    await db.prepare(
      `INSERT INTO finans_odeme
         (id, tutar, tarih, kasa_banka_hesabi_id, odeme_yontemi_id, cari_tip, cari_id, aciklama, created_by, created_at)
       VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)`
    ).run(id, d.tutar, d.tarih, d.kasa_banka_hesabi_id, d.odeme_yontemi_id || null, d.cari_tip, d.cari_id, d.aciklama || null, user.id, now);

    for (const eslesme of d.fatura_eslesme || []) {
      await db.prepare(
        `INSERT INTO finans_odeme_fatura (id, odeme_id, fatura_id, tutar, created_at) VALUES (?, ?, ?, ?, ?)`
      ).run(uuidv4(), id, eslesme.fatura_id, eslesme.tutar, now);

      const fatura = await db.prepare(`SELECT genel_toplam FROM finans_fatura WHERE id = ?`).get(eslesme.fatura_id) as
        { genel_toplam: number } | undefined;
      if (fatura) {
        const toplamEslesen = await db.prepare(
          `SELECT COALESCE(SUM(tutar), 0) AS toplam FROM finans_odeme_fatura WHERE fatura_id = ?`
        ).get(eslesme.fatura_id) as { toplam: number };
        let yeniDurum = "odenmedi";
        const toplam = Number(toplamEslesen.toplam);
        const genelToplam = Number(fatura.genel_toplam);
        if (toplam > genelToplam) yeniDurum = "fazla_odendi";
        else if (toplam === genelToplam) yeniDurum = "odendi";
        else if (toplam > 0) yeniDurum = "kismen_odendi";
        await db.prepare(`UPDATE finans_fatura SET odeme_durumu = ? WHERE id = ?`).run(yeniDurum, eslesme.fatura_id);
      }
    }

    return NextResponse.json({ ok: true, data: { id } }, { status: 201 });
  } catch (e) { return apiError(e); }
}
```

- [ ] **Step 2: Delete route'unu yaz**

`app/api/finans/odeme/[id]/route.ts` — sadece `DELETE`: silmeden önce
ilişkili `finans_odeme_fatura` satırlarını sil (ya da `ON DELETE CASCADE`
zaten hallediyor, migration'a bak), sonra etkilenen faturaların
`odeme_durumu`'nu yeniden hesapla (kalan eşleşme toplamına göre), en son
`finans_odeme` satırını sil. `finans_odeme:read` yeterli değil, silme için
ayrı bir `:delete` action yok bu kaynakta (Task 2'de sadece `read`/`create`
tanımlandı) — silme işlemini `finans_odeme:create` iznine bağla (bu depoda
`create` iznine sahip birinin kendi oluşturduğu kaydı geri alabilmesi makul,
ayrı bir `:delete` action'ı bu MVP'de gereksiz karmaşıklık).

- [ ] **Step 3: Sayfayı yaz**

`app/finans/odemeler/page.tsx` — form: `cari_tip`+`cari_id`, `tutar`, `tarih`
(varsayılan `todayIstanbul()`), `kasa_banka_hesabi_id` (dropdown),
`odeme_yontemi_id` (dropdown, opsiyonel), `aciklama`. Cari seçildikten sonra,
o cariye ait `odeme_durumu` "odenmedi" veya "kismen_odendi" olan açık
faturaları (`GET /api/finans/fatura` sonucunu `cari_id` ile client-side
filtrele) bir checkbox listesi olarak göster, her seçilen fatura için
eşleştirilecek tutarı giren bir input (varsayılan: faturanın kalan bakiyesi)
— toplam eşleştirilen tutar, ödemenin `tutar` alanını aşamaz (client-side
uyarı, sunucu da reddetmeli ama bu fazda sunucu tarafı toplam kontrolü basit
tutuluyor, sadece kayıt oluşturuyor — aşırı eşleştirme durumunda fatura
`fazla_odendi` olarak işaretlenir, bu kabul edilebilir bir MVP davranışı).
Liste: cari adı, tutar, tarih, kasa/banka hesabı, kaç faturaya eşleştiği
(`finans_odeme_fatura` sayısı — GET route'una bu sayıyı da ekle:
`(SELECT COUNT(*) FROM finans_odeme_fatura WHERE odeme_id = o.id) AS
eslesen_fatura_sayisi`).

- [ ] **Step 4: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```

- [ ] **Step 5: Canlı doğrulama**

Task 6'da oluşturulan 1200 TL'lik satış faturasına karşılık, aynı cariye
1200 TL'lik bir ödeme oluştur, faturayı tam eşleştir. DB'den doğrula:

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node -e "
const mysql = require('mysql2/promise');
(async () => {
  const conn = await mysql.createConnection({ host: '127.0.0.1', port: 3307, user: 'aycanops', password: 'Kayra2190.', database: 'aycanops_db' });
  const [rows] = await conn.query('SELECT odeme_durumu, genel_toplam FROM finans_fatura ORDER BY created_at DESC LIMIT 1');
  console.log(rows);
  await conn.end();
})().catch(e => { console.error(e.message); process.exit(1); });
"
```
Beklenen: `odeme_durumu: 'odendi'`.

- [ ] **Step 6: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add app/api/finans/odeme app/finans/odemeler
git commit -m "Finans Faz 2: Ödeme modülü + fatura eşleştirme"
```

---

### Task 10: Banka hareketi (manuel/CSV import + eşleştirme)

**Files:**
- Create: `app/api/finans/banka-hareketi/route.ts`
- Create: `app/api/finans/banka-hareketi/[id]/route.ts`
- Create: `app/finans/banka-hareketleri/page.tsx`

**Interfaces:**
- Consumes: `finansBankaHareketiSchema` (Task 3), `GET /api/finans/kasa-banka`
  (Faz 1), `GET /api/finans/fatura` (Task 6), `GET /api/finans/odeme`
  (Task 9) — manuel eşleştirme dropdown'ları için.
- Produces: bağımsız modül.

- [ ] **Step 1-2: Route'ları yaz**

`app/api/finans/banka-hareketi/route.ts` (GET/POST) — POST tek satır ekler
(manuel giriş) VEYA `raw.satirlar` bir dizi ise (CSV'den parse edilmiş,
parse işlemi CLIENT-SIDE yapılır — sunucu sadece hazır satır dizisini
INSERT eder, CSV parse mantığı Task olarak sunucuda YOK, basit bir
`FileReader`+manuel virgül-split client-side yeterli, bu fazda tam bir CSV
kütüphanesi gerekmez) her satır için ayrı `finans_banka_hareketi` INSERT'i
yapar (`eslesen_tip`/`eslesen_id` başta NULL). `app/api/finans/banka-hareketi/
[id]/route.ts` (PUT) — sadece `action: "eslestir"` ile `eslesen_tip` +
`eslesen_id` günceller (manuel eşleştirme onayı).

- [ ] **Step 3: Sayfayı yaz**

`app/finans/banka-hareketleri/page.tsx`:
- Üstte hesap seçimi (Faz 1'in kasa-banka listesinden, sadece tip="banka"
  olanlar).
- "CSV Yükle" butonu: dosya seçtirir, `FileReader.readAsText`, basit
  virgülle ayırma (`tarih,aciklama,tutar` sütun sırası varsayılır — başlık
  satırını atla), her satırı `{tarih, aciklama, tutar: Math.abs(tutar), yon:
  tutar >= 0 ? "gelen" : "giden"}` olarak parse edip toplu POST eder.
  "Manuel Ekle" butonu: tek satırlık form (tarih/aciklama/tutar/yön).
- Liste: tarih, açıklama, tutar (yön'e göre yeşil/kırmızı), eşleşme durumu
  ("Eşleşmedi" / "Fatura: <no>" / "Ödeme: <id kısaltması>"). Eşleşmemiş
  satırlarda "Eşleştir" butonu — tıklanınca fatura/ödeme listesinden arama
  yapılan basit bir seçim modalı açılır (aynı tutar+yakın tarihli kayıtları
  öne çıkarmak gibi "otomatik eşleştirme önerisi" bu fazın kapsamında DEĞİL
  — spec'in "otomatik eşleştirme önerileri Faz 2'de yok" kısıtına uygun,
  sadece manuel arama+seç).

- [ ] **Step 4: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```

- [ ] **Step 5: Canlı doğrulama**

"Manuel Ekle" ile bir banka hareketi oluştur (tarih=bugün, açıklama="Test
Havale", tutar=1200, yön=gelen), listede görünmeli. "Eşleştir" ile Task 6'da
oluşturulan faturaya eşleştir, DB'de `eslesen_tip='fatura'` ve doğru
`eslesen_id` görünmeli.

- [ ] **Step 6: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add app/api/finans/banka-hareketi app/finans/banka-hareketleri
git commit -m "Finans Faz 2: Banka hareketi (manuel/CSV import + eşleştirme)"
```

---

### Task 11: Nav entegrasyonu

**Files:**
- Modify: `components/Nav.tsx`

**Interfaces:**
- Consumes: mevcut `FINANS_LINKS` dizisi (Faz 1'de zaten var, Faz 1'in
  gelir-gider/masraf-talebi linkleri de bu diziye eklenmişti).

- [ ] **Step 1: `FINANS_LINKS`'e 4 yeni link ekle**

```ts
  { href: "/finans/faturalar", label: "Faturalar", Icon: IconDocument },
  { href: "/finans/fisler", label: "Fişler", Icon: IconClipboard2 },
  { href: "/finans/belgeler", label: "Finans Belgeleri", Icon: IconDocument },
  { href: "/finans/odemeler", label: "Ödemeler", Icon: IconCoin },
```

(`IconDocument`, `IconClipboard2`, `IconCoin` zaten import edilmiş — Faz
1'de doğrulandı. Banka hareketleri linki BİLİNÇLİ OLARAK eklenmiyor —
spec'te bu ekran kasa-banka hesap detayına gömülü bir alt-sekme olarak
tasarlandıysa ayrı bir üst menü linki gerekmez; implementer bu task'ı
uygularken karar verirse `/finans/banka-hareketleri` linkini de aynı şekilde
ekleyebilir, bu bir engelleyici değil.)

- [ ] **Step 2: `NAV_PERMISSION_BY_HREF`'e ekle**

```ts
  "/finans/faturalar": "finans_fatura:read",
  "/finans/fisler": "finans_fis:read",
  "/finans/belgeler": "finans_belge:read",
  "/finans/odemeler": "finans_odeme:read",
  "/finans/banka-hareketleri": "finans_banka_hareketi:read",
```

- [ ] **Step 3: Tip kontrolü**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops && node node_modules/typescript/bin/tsc --noEmit
```

- [ ] **Step 4: Canlı doğrulama**

Admin olarak sidebar'daki "Finans" grubunda yeni linklerin göründüğünü,
tıklayınca doğru sayfaların açıldığını doğrula.

- [ ] **Step 5: Commit**

```bash
cd /Users/kayraisbilir/Documents/tasinios/aycanops/aycanops
git add components/Nav.tsx
git commit -m "Finans Faz 2: sidebar'a fatura/fiş/belge/ödeme linkleri ekle"
```

---

### Task 12: Uçtan uca smoke test

**Files:** yok (sadece doğrulama)

- [ ] **Step 1:** Tüm yeni sayfaları sırayla ziyaret et (200 dönmeli,
  konsol hatasız): `/admin/finans/belge-turu`, `/finans/faturalar`,
  `/finans/fisler`, `/finans/belgeler`, `/finans/odemeler`,
  `/finans/banka-hareketleri`.
- [ ] **Step 2:** `node node_modules/typescript/bin/tsc --noEmit` son kez
  çalıştır, sıfır hata olmalı.
- [ ] **Step 3:** Dev log'da hata olmadığını doğrula (`grep -i error
  /tmp/aycanops-dev.log`, `ECONNREFUSED` gibi tünel kaynaklı geçmiş
  girdiler hariç).
- [ ] **Step 4:** Faz 2'yi tamamlandı işaretle. Faz 3 (Cari Detay + Bütçe +
  Tam Muhasebe) için ayrı bir plan dosyası bu tamamlandıktan sonra yazılacak.
