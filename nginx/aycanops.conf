# HTTP → redirect to HTTPS (active after SSL kurulumu)
# İlk kurulumda bu blok kapalı tutulur; aşağıdaki HTTP blok açık kalır.
# server {
#     listen 80;
#     server_name aycanops.agno.digital;
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }

# HTTPS (SSL kurulumu sonrası bu bloğu aç, HTTP bloğunu kapat)
# server {
#     listen 443 ssl;
#     server_name aycanops.agno.digital;
#     ssl_certificate /etc/letsencrypt/live/aycanops.agno.digital/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/aycanops.agno.digital/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#     location / {
#         proxy_pass http://aycanops_app:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_cache_bypass $http_upgrade;
#         client_max_body_size 20m;
#     }
# }

# --- Faz 1: HTTP direkt proxy (cert yokken) ---
server {
    listen 80;
    server_name aycanops.agno.digital _;

    # certbot challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://aycanops_app:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        client_max_body_size 20m;
    }
}
